# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-revenue-trend.spec.ts >> Backend (Konza) â€” Dashboard Revenue Trend >> TC-DREVT-014 â€” Math Consistency: successful + failed must equal totalAttempts at every index
- Location: tests/API/backend-tests/backend-dashboard-revenue-trend.spec.ts:1134:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 503
```

# Test source

```ts
  1048 |       logger.info('Asserting: BUG-REVT-11b: Destructive methods must not be COR');
  1049 |       expect.soft(allowsDelete, '[BUG-REVT-11b] CORS must not allow DELETE on GET-only endpoint').toBe(false);
  1050 |       expect.soft(allowsPost,   '[BUG-REVT-11b] CORS must not allow POST on GET-only endpoint').toBe(false);
  1051 |       logger.pass('All assertions passed');
  1052 |     });
  1053 |   });
  1054 | 
  1055 |   // â”€â”€ TC-DREVT-013 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  1056 |   test('TC-DREVT-013 â€” Array Integrity: all arrays same length, no null/undefined entries', async ({ logger }) => {
  1057 |     backendLabels('TC-DREVT-013', 'Array Integrity', 'normal');
  1058 | 
  1059 |     let res: supertest.Response;
  1060 | 
  1061 |     await logger.step('Step 1 â€” Fetch revenue-trend response', async () => {
  1062 |       logger.info(`GET ${ENDPOINT}`);
  1063 |       res = await get();
  1064 |       logger.pass('HTTP ' + res.status + ' received');
  1065 |       attachResponse('TC-DREVT-013', res);
  1066 |     });
  1067 | 
  1068 |     await logger.step('Step 2 â€” Validate HTTP 200', async () => {
  1069 |       logger.info('Asserting: Validate HTTP 200');
  1070 |       expect(res!.status).toBe(200);
  1071 |       logger.pass('All assertions passed');
  1072 |     });
  1073 | 
  1074 |     await logger.step('Step 3 â€” All 4 arrays must have identical length', async () => {
  1075 |       const d = res!.body.data as RevenueTrendData;
  1076 |       const len = d.labels.length;
  1077 | 
  1078 |       allure.parameter('labels.length',        String(d.labels.length));
  1079 |       allure.parameter('totalAttempts.length', String(d.totalAttempts.length));
  1080 |       allure.parameter('successful.length',    String(d.successful.length));
  1081 |       allure.parameter('failed.length',        String(d.failed.length));
  1082 | 
  1083 |       logger.info('Asserting: response structure and values');
  1084 |       expect(d.totalAttempts.length, 'totalAttempts must be same length as labels').toBe(len);
  1085 |       expect(d.successful.length,    'successful must be same length as labels').toBe(len);
  1086 |       expect(d.failed.length,        'failed must be same length as labels').toBe(len);
  1087 |       logger.pass('All assertions passed');
  1088 |     });
  1089 | 
  1090 |     await logger.step('Step 4 â€” No null/undefined/NaN entries in numeric arrays', async () => {
  1091 |       const d = res!.body.data as RevenueTrendData;
  1092 |       const arrays: [string, unknown[]][] = [
  1093 |         ['totalAttempts', d.totalAttempts],
  1094 |         ['successful',    d.successful],
  1095 |         ['failed',        d.failed],
  1096 |       ];
  1097 | 
  1098 |       for (const [name, arr] of arrays) {
  1099 |         arr.forEach((val, i) => {
  1100 |           logger.info('Asserting: response structure and values');
  1101 |           expect(val, `${name}[${i}] must not be null`).not.toBeNull();
  1102 |           expect(val, `${name}[${i}] must not be undefined`).toBeDefined();
  1103 |           expect(Number.isNaN(val), `${name}[${i}] must not be NaN`).toBe(false);
  1104 |           expect(typeof val, `${name}[${i}] must be a number`).toBe('number');
  1105 |           expect(val as number, `${name}[${i}] must be >= 0`).toBeGreaterThanOrEqual(0);
  1106 |         });
  1107 |       }
  1108 | 
  1109 |       d.labels.forEach((lbl, i) => {
  1110 |         expect(typeof lbl, `labels[${i}] must be a string`).toBe('string');
  1111 |         expect((lbl as string).length, `labels[${i}] must not be empty`).toBeGreaterThan(0);
  1112 |       });
  1113 |       logger.pass('All assertions passed');
  1114 |     });
  1115 | 
  1116 |     await logger.step('Step 5 â€” summary must have exactly 3 items with required fields', async () => {
  1117 |       const d = res!.body.data as RevenueTrendData;
  1118 |       logger.info('Asserting: response structure and values');
  1119 |       expect(d.summary.length, 'summary must have exactly 3 items').toBe(3);
  1120 | 
  1121 |       const requiredFields = ['value', 'label', 'change'];
  1122 |       for (const [i, item] of d.summary.entries()) {
  1123 |         for (const field of requiredFields) {
  1124 |           expect(item, `summary[${i}] must have "${field}"`).toHaveProperty(field);
  1125 |         }
  1126 |         expect(typeof item.label, `summary[${i}].label must be a string`).toBe('string');
  1127 |         expect((item.label as string).length, `summary[${i}].label must not be empty`).toBeGreaterThan(0);
  1128 |       }
  1129 |       logger.pass('All assertions passed');
  1130 |     });
  1131 |   });
  1132 | 
  1133 |   // â”€â”€ TC-DREVT-014 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  1134 |   test('TC-DREVT-014 â€” Math Consistency: successful + failed must equal totalAttempts at every index', async ({ logger }) => {
  1135 |     backendLabels('TC-DREVT-014', 'Math Consistency â€” per-month totals', 'critical');
  1136 | 
  1137 |     let res: supertest.Response;
  1138 | 
  1139 |     await logger.step('Step 1 â€” Fetch revenue-trend response', async () => {
  1140 |       logger.info(`GET ${ENDPOINT}`);
  1141 |       res = await get();
  1142 |       logger.pass('HTTP ' + res.status + ' received');
  1143 |       attachResponse('TC-DREVT-014', res);
  1144 |     });
  1145 | 
  1146 |     await logger.step('Step 2 â€” Validate HTTP 200', async () => {
  1147 |       logger.info('Asserting: Validate HTTP 200');
> 1148 |       expect(res!.status).toBe(200);
       |                           ^ Error: expect(received).toBe(expected) // Object.is equality
  1149 |       logger.pass('All assertions passed');
  1150 |     });
  1151 | 
  1152 |     await logger.step('Step 3 â€” Per-index: successful[i] + failed[i] must equal totalAttempts[i]', async () => {
  1153 |       const d = res!.body.data as RevenueTrendData;
  1154 | 
  1155 |       d.labels.forEach((label, i) => {
  1156 |         const ta = d.totalAttempts[i];
  1157 |         const s  = d.successful[i];
  1158 |         const f  = d.failed[i];
  1159 |         const sum = s + f;
  1160 | 
  1161 |         allure.parameter(`[${i}] ${label}: total`, String(ta));
  1162 |         allure.parameter(`[${i}] ${label}: success+failed`, `${s}+${f}=${sum}`);
  1163 | 
  1164 |         if (ta !== sum) {
  1165 |           flagIssue('TC-DREVT-014', 'BUG-REVT-MATH',
  1166 |             `Index ${i} (${label}): totalAttempts=${ta} but successful+failed=${s}+${f}=${sum} â€” mismatch of ${Math.abs(ta - sum)}`,
  1167 |             { index: i, label, totalAttempts: ta, successful: s, failed: f, sum, diff: ta - sum },
  1168 |           );
  1169 |         }
  1170 | 
  1171 |         logger.info('Asserting: response structure and values');
  1172 |         expect(sum, `[${i}] ${label}: successful(${s}) + failed(${f}) must equal totalAttempts(${ta})`).toBe(ta);
  1173 |       });
  1174 |       logger.pass('All assertions passed');
  1175 |     });
  1176 | 
  1177 |     await logger.step('Step 4 â€” Array sums must match summary.points values', async () => {
  1178 |       const d = res!.body.data as RevenueTrendData;
  1179 |       const sumTA = d.totalAttempts.reduce((a, b) => a + b, 0);
  1180 |       const sumS  = d.successful.reduce((a, b) => a + b, 0);
  1181 |       const sumF  = d.failed.reduce((a, b) => a + b, 0);
  1182 | 
  1183 |       allure.parameter('totalAttempts array sum', String(sumTA));
  1184 |       allure.parameter('successful array sum',    String(sumS));
  1185 |       allure.parameter('failed array sum',        String(sumF));
  1186 | 
  1187 |       const ptTA = Number(d.summary[0]?.points ?? NaN);
  1188 |       const ptS  = Number(d.summary[1]?.points ?? NaN);
  1189 |       const ptF  = Number(d.summary[2]?.points ?? NaN);
  1190 | 
  1191 |       allure.parameter('summary[0].points (Total Attempts)', String(ptTA));
  1192 |       allure.parameter('summary[1].points (Successful)',      String(ptS));
  1193 |       allure.parameter('summary[2].points (Failed)',          String(ptF));
  1194 | 
  1195 |       logger.info('Asserting: response structure and values');
  1196 |       expect(sumTA, `totalAttempts array sum (${sumTA}) must match summary[0].points (${ptTA})`).toBe(ptTA);
  1197 |       expect(sumS,  `successful array sum (${sumS}) must match summary[1].points (${ptS})`).toBe(ptS);
  1198 |       expect(sumF,  `failed array sum (${sumF}) must match summary[2].points (${ptF})`).toBe(ptF);
  1199 |       logger.pass('All assertions passed');
  1200 |     });
  1201 | 
  1202 |     await logger.step('Step 5 â€” KES cross-validation: Total âˆ’ Successful must equal Failed KES', async () => {
  1203 |       const d = res!.body.data as RevenueTrendData;
  1204 |       const parseKES = (s: unknown) => Number(String(s).replace('KES ', '').replace(/,/g, ''));
  1205 | 
  1206 |       const totKES  = parseKES(d.summary[0]?.value);
  1207 |       const sucKES  = parseKES(d.summary[1]?.value);
  1208 |       const failKES = parseKES(d.summary[2]?.value);
  1209 |       const diff    = totKES - sucKES;
  1210 | 
  1211 |       allure.parameter('Total KES',          totKES.toLocaleString());
  1212 |       allure.parameter('Successful KES',     sucKES.toLocaleString());
  1213 |       allure.parameter('Failed KES',         failKES.toLocaleString());
  1214 |       allure.parameter('Total âˆ’ Successful', diff.toLocaleString());
  1215 |       allure.parameter('Matches Failed KES', String(diff === failKES));
  1216 | 
  1217 |       if (!isNaN(totKES) && !isNaN(sucKES) && !isNaN(failKES)) {
  1218 |         logger.info('Asserting: KES cross-validation: Total âˆ’ Successful must e');
  1219 |         expect(diff, `Total KES (${totKES}) âˆ’ Successful KES (${sucKES}) must equal Failed KES (${failKES})`).toBe(failKES);
  1220 |       }
  1221 |       logger.pass('All assertions passed');
  1222 |     });
  1223 |   });
  1224 | 
  1225 |   // â”€â”€ TC-DREVT-015 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  1226 |   test('TC-DREVT-015 â€” Response-Time SLA: must respond within 2 000 ms', async ({ logger }) => {
  1227 |     backendLabels('TC-DREVT-015', 'Response-Time SLA', 'minor');
  1228 | 
  1229 |     const SLA_MS = 2000;
  1230 |     let durationMs = 0;
  1231 |     let res: supertest.Response;
  1232 | 
  1233 |     await logger.step('Step 1 â€” Measure round-trip time', async () => {
  1234 |       const t0 = Date.now();
  1235 |       logger.info(`GET ${ENDPOINT}`);
  1236 |       res = await get();
  1237 |       logger.pass('HTTP ' + res.status + ' received');
  1238 |       durationMs = Date.now() - t0;
  1239 | 
  1240 |       console.log(`[TC-DREVT-015] Response time: ${durationMs}ms  (SLA: ${SLA_MS}ms)`);
  1241 |       allure.parameter('Response Time (ms)', String(durationMs));
  1242 |       allure.parameter('SLA Threshold (ms)', String(SLA_MS));
  1243 |       allure.parameter('SLA Pass',           String(durationMs < SLA_MS));
  1244 |     });
  1245 | 
  1246 |     await logger.step('Step 2 â€” Validate HTTP 200', async () => {
  1247 |       logger.info('Asserting: Validate HTTP 200');
  1248 |       expect(res!.status).toBe(200);
```