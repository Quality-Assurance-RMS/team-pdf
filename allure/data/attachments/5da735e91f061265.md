# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/Transactions/table-rows-loaded.spec.ts >> Transactions — Table Rows Loaded >> TC2 — Table loads with at least one transaction row
- Location: tests/UI/Transactions/table-rows-loaded.spec.ts:20:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('table tbody tr').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('table tbody tr').first()

```

```yaml
- complementary:
  - img "eCitizen"
  - navigation:
    - link "Dashboard":
      - /url: /dashboard
      - img
      - text: Dashboard
    - link "Transactions":
      - /url: /transaction
      - img
      - text: Transactions
    - link "Settlements":
      - /url: /settlements
      - img
      - text: Settlements
    - link "Reports":
      - /url: /reports
      - img
      - text: Reports
    - link "Settings":
      - /url: /settings
      - img
      - text: Settings
  - button "Logout":
    - img
    - text: Logout
  - button "Collapse sidebar":
    - img
- banner:
  - button "Notifications": "3"
  - text: A Ashil ADMIN PLATFORM
- main:
  - heading "Transactions" [level=2]
  - paragraph: 0 transactions
  - textbox "Search Transaction id..."
  - img
  - combobox:
    - option "Today" [selected]
    - option "Last 7 Days"
    - option "Last 30 Days"
    - option "Last 90 Days"
    - option "Custom Range"
- region "Notifications alt+T"
```

# Test source

```ts
  1  | /**
  2  |  * @file table-rows-loaded.spec.ts
  3  |  * @testId TXN-TBL-TC2
  4  |  * @feature Transactions — Table Data
  5  |  * @priority P0 / Blocker
  6  |  *
  7  |  * Verifies that the transactions table loads with at least one data row
  8  |  * and that each visible row contains a non-empty Transaction ID.
  9  |  */
  10 | 
  11 | import { test, expect } from '../fixtures';
  12 | import type { TestInfo } from '@playwright/test';
  13 | import { allure } from 'allure-playwright';
  14 | import { TransactionPage } from '../../pages/TransactionPage';
  15 | import { attachScreenshot } from '../../utils/screenshotHelper';
  16 | 
  17 | test.describe('Transactions — Table Rows Loaded', () => {
  18 |   test.setTimeout(120000);
  19 | 
  20 |   test('TC2 — Table loads with at least one transaction row', async ({ page, logger }, testInfo: TestInfo) => {
  21 |     await allure.description('Verifies the transactions table is populated with data rows after page load.');
  22 |     await allure.label('feature',  'Transactions');
  23 |     await allure.label('story',    'Table Data');
  24 |     await allure.label('severity', 'blocker');
  25 |     await allure.label('priority', 'P0');
  26 |     await allure.label('testId',   'TXN-TBL-TC2');
  27 | 
  28 |     const txnPage = new TransactionPage(page);
  29 | 
  30 |     await logger.step('Step 1 — Navigate to Transactions page', async () => {
  31 |       logger.info('Opening the Transactions page');
  32 |       await txnPage.goto();
  33 |       logger.pass('Navigation to Transactions page initiated successfully');
  34 |       await attachScreenshot(page, testInfo, '01 — Page loading');
  35 |     });
  36 | 
  37 |     await logger.step('Step 2 — Verify page loads successfully', async () => {
  38 |       logger.info('Waiting for all Transactions page elements to render');
  39 |       await txnPage.assertPageLoaded();
  40 |       logger.pass('Transactions page is fully loaded with all required elements');
  41 |       await attachScreenshot(page, testInfo, '02 — Page loaded and ready');
  42 |     });
  43 | 
  44 |     await logger.step('Step 3 — At least one table row is present', async () => {
  45 |       logger.info('Checking table data is loaded');
  46 |       const rows = page.locator('table tbody tr');
> 47 |       await expect(rows.first()).toBeVisible({ timeout: 15000 });
     |                                  ^ Error: expect(locator).toBeVisible() failed
  48 |       logger.pass('Table has rendered with data rows');
  49 |       await attachScreenshot(page, testInfo, '03 — Table row visible', rows.first());
  50 |     });
  51 | 
  52 |     await logger.step('Step 4 — First row Transaction ID is non-empty', async () => {
  53 |       logger.info('Reading first Transaction ID from the table');
  54 |       const firstId = await txnPage.getFirstTransactionId();
  55 |       expect(firstId).toBeTruthy();
  56 |       logger.pass('Transaction ID captured: ' + firstId);
  57 |       allure.parameter('First Transaction ID', firstId);
  58 |       await attachScreenshot(page, testInfo, '04 — First Transaction ID non-empty');
  59 |     });
  60 |   });
  61 | });
  62 | 
```