# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-today-payments.spec.ts >> Backend (Konza) â€" Dashboard Today Payments >> TC-DTPAY-011 â€" BUG-TPAY-11: Percentage format must be consistent across sections
- Location: tests/API/backend-tests/backend-dashboard-today-payments.spec.ts:980:7

# Error details

```
Error: payments.postiveValue "" must match N% or N.N% format

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  940  |       logger.pass('HTTP ' + resUnknown.status + ' received');
  941  | 
  942  |       console.log(`[TC-DTPAY-010] ?foo=bar  -> ${resUnknown.status}`);
  943  |       allure.parameter('?foo=bar&baz=qux status', String(resUnknown.status));
  944  |     });
  945  | 
  946  |     await logger.step('Step 3 â€" BUG-TPAY-09: Unknown params should not silently return 200', async () => {
  947  |       // The ?date param is silently ignored â€" if date filtering is intended, this is a missing feature
  948  |       // If date filtering is NOT intended, unknown params should return 400 Bad Request
  949  |       if (resDate!.status === 200) {
  950  |         flagIssue('TC-DTPAY-010', 'BUG-TPAY-09a',
  951  |           'GET /today-payments?date=2026-06-01 returns 200 and silently ignores the date parameter â€" ' +
  952  |           'if date filtering is supported, data should reflect that date; if not, the server should return 400 for unknown params',
  953  |           { actual_status: 200, query_param: 'date=2026-06-01', body_unchanged: true },
  954  |         );
  955  |       }
  956  | 
  957  |       if (resUnknown!.status === 200) {
  958  |         flagIssue('TC-DTPAY-010', 'BUG-TPAY-09b',
  959  |           'GET /today-payments?foo=bar returns 200 â€" unknown query parameters should return 400 Bad Request, not silently succeed',
  960  |           { actual_status: 200, query_param: 'foo=bar&baz=qux' },
  961  |         );
  962  |       }
  963  | 
  964  |       logger.info('Asserting: unknown query parameters are rejected with 400, not silently ignored');
  965  |       expect.soft(resDate!.status,
  966  |         '[BUG-TPAY-09a] ?date= parameter is silently ignored â€" endpoint should filter by date or reject the param with 400',
  967  |       ).not.toBe(200);
  968  | 
  969  |       expect.soft(resUnknown!.status,
  970  |         '[BUG-TPAY-09b] Unknown query params should return 400, not 200',
  971  |       ).not.toBe(200);
  972  | 
  973  |       logger.pass('All assertions passed');
  974  |       attachResponse('TC-DTPAY-010', resDate!,    { scenario: '?date=2026-06-01', bug: 'BUG-TPAY-09' });
  975  |       attachResponse('TC-DTPAY-010', resUnknown!, { scenario: '?foo=bar', bug: 'BUG-TPAY-09' });
  976  |     });
  977  |   });
  978  | 
  979  |   // â"€â"€ TC-DTPAY-011 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  980  |   test('TC-DTPAY-011 â€" BUG-TPAY-11: Percentage format must be consistent across sections', async ({ logger }) => {
  981  |     backendLabels('TC-DTPAY-011', 'BUG-TPAY-11 â€" Inconsistent Percentage Format', 'minor');
  982  | 
  983  |     let res: supertest.Response;
  984  | 
  985  |     await logger.step('Step 1 â€" Fetch today-payments response', async () => {
  986  |       logger.info('GET /api/v1/dashboard/today-payments');
  987  |       res = await get();
  988  |       logger.pass('HTTP ' + res.status + ' received');
  989  |       attachResponse('TC-DTPAY-011', res, { bug: 'BUG-TPAY-11' });
  990  |     });
  991  | 
  992  |     await logger.step('Step 2 â€" Validate HTTP 200', async () => {
  993  |       logger.info('Asserting: HTTP 200');
  994  |       expect(res!.status).toBe(200);
  995  |       logger.pass('All assertions passed');
  996  |     });
  997  | 
  998  |     await logger.step('Step 3 â€" BUG-TPAY-11: All postiveValue fields must use consistent decimal formatting', async () => {
  999  |       const payPct = String(res!.body.data.payments.postiveValue ?? '');
  1000 |       const refPct = String(res!.body.data.refunds.postiveValue  ?? '');
  1001 |       const revPct = String(res!.body.data.revenue.postiveValue  ?? '');
  1002 | 
  1003 |       allure.parameter('payments.postiveValue', payPct);
  1004 |       allure.parameter('refunds.postiveValue',  refPct);
  1005 |       allure.parameter('revenue.postiveValue',  revPct);
  1006 | 
  1007 |       console.log(`[TC-DTPAY-011] payments: "${payPct}", refunds: "${refPct}", revenue: "${revPct}"`);
  1008 | 
  1009 |       // Check format consistency: all should use the same decimal format
  1010 |       const PCT_WITH_DECIMAL = /^-?\d+\.\d+%$/;  // e.g. "-100.0%" or "0.0%"
  1011 |       const PCT_NO_DECIMAL   = /^-?\d+%$/;        // e.g. "0%" or "-100%"
  1012 | 
  1013 |       const payHasDecimal = PCT_WITH_DECIMAL.test(payPct);
  1014 |       const refHasDecimal = PCT_WITH_DECIMAL.test(refPct);
  1015 |       const revHasDecimal = PCT_WITH_DECIMAL.test(revPct);
  1016 | 
  1017 |       const allDecimal   = payHasDecimal && refHasDecimal && revHasDecimal;
  1018 |       const allNoDecimal = !payHasDecimal && !refHasDecimal && !revHasDecimal;
  1019 |       const isConsistent = allDecimal || allNoDecimal;
  1020 | 
  1021 |       allure.parameter('payments has decimal', String(payHasDecimal));
  1022 |       allure.parameter('refunds has decimal',  String(refHasDecimal));
  1023 |       allure.parameter('revenue has decimal',  String(revHasDecimal));
  1024 |       allure.parameter('Format consistent',    String(isConsistent));
  1025 | 
  1026 |       if (!isConsistent) {
  1027 |         flagIssue('TC-DTPAY-011', 'BUG-TPAY-11',
  1028 |           `Inconsistent percentage format: payments="${payPct}" (decimal), refunds="${refPct}" (no decimal), revenue="${revPct}" â€" all 3 sections must use the same format`,
  1029 |           { payments: payPct, refunds: refPct, revenue: revPct, expected: 'consistent decimal format, e.g. all "0.0%" or all "0%"' },
  1030 |         );
  1031 |       }
  1032 | 
  1033 |       logger.info('Asserting: all postiveValue fields use consistent decimal formatting and match percentage pattern');
  1034 |       expect.soft(isConsistent,
  1035 |         `[BUG-TPAY-11] Percentage format is inconsistent: payments="${payPct}", refunds="${refPct}", revenue="${revPct}" â€" must be uniform`,
  1036 |       ).toBe(true);
  1037 | 
  1038 |       // All values must match percentage pattern
  1039 |       const PCT_PATTERN = /^-?\d+(\.\d+)?%$/;
> 1040 |       expect(PCT_PATTERN.test(payPct), `payments.postiveValue "${payPct}" must match N% or N.N% format`).toBe(true);
       |                                                                                                          ^ Error: payments.postiveValue "" must match N% or N.N% format
  1041 |       expect(PCT_PATTERN.test(refPct), `refunds.postiveValue "${refPct}" must match N% or N.N% format`).toBe(true);
  1042 |       expect(PCT_PATTERN.test(revPct), `revenue.postiveValue "${revPct}" must match N% or N.N% format`).toBe(true);
  1043 |       logger.pass('All assertions passed');
  1044 |     });
  1045 |   });
  1046 | 
  1047 |   // â"€â"€ TC-DTPAY-012 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  1048 |   test('TC-DTPAY-012 â€" Response-Time SLA: dashboard endpoint must respond within 2 000 ms', async ({ logger }) => {
  1049 |     backendLabels('TC-DTPAY-012', 'Response-Time SLA', 'minor');
  1050 | 
  1051 |     const SLA_MS = 2000;
  1052 |     let durationMs = 0;
  1053 |     let res: supertest.Response;
  1054 | 
  1055 |     await logger.step('Step 1 â€" Measure round-trip response time', async () => {
  1056 |       logger.info('GET /api/v1/dashboard/today-payments (measuring response time)');
  1057 |       const t0 = Date.now();
  1058 |       res = await get();
  1059 |       durationMs = Date.now() - t0;
  1060 |       logger.pass('HTTP ' + res.status + ' received');
  1061 | 
  1062 |       console.log(`[TC-DTPAY-012] Response time: ${durationMs}ms  (SLA: ${SLA_MS}ms)`);
  1063 |       allure.parameter('Response Time (ms)',  String(durationMs));
  1064 |       allure.parameter('SLA Threshold (ms)',  String(SLA_MS));
  1065 |       allure.parameter('SLA Pass',            String(durationMs < SLA_MS));
  1066 |     });
  1067 | 
  1068 |     await logger.step('Step 2 â€" Validate HTTP 200', async () => {
  1069 |       logger.info('Asserting: HTTP 200');
  1070 |       expect(res!.status).toBe(200);
  1071 |       logger.pass('All assertions passed');
  1072 |     });
  1073 | 
  1074 |     await logger.step(`Step 3 â€" Response time must be under ${SLA_MS}ms`, async () => {
  1075 |       if (durationMs >= SLA_MS) {
  1076 |         flagIssue('TC-DTPAY-012', 'PERF-TPAY-01',
  1077 |           `Response time ${durationMs}ms exceeds ${SLA_MS}ms SLA for a dashboard widget endpoint`,
  1078 |           { actual_ms: durationMs, sla_ms: SLA_MS, endpoint: `GET ${ENDPOINT}` },
  1079 |         );
  1080 |       }
  1081 |       logger.info(`Asserting: response time ${durationMs}ms is under the ${SLA_MS}ms SLA`);
  1082 |       expect(durationMs, `Response time ${durationMs}ms must be < ${SLA_MS}ms`).toBeLessThan(SLA_MS);
  1083 |       logger.pass('All assertions passed');
  1084 |     });
  1085 |   });
  1086 | 
  1087 | });
  1088 | 
```