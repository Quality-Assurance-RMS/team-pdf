# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.konza.ts >> authenticate via TaifaSSO and save Konza session
- Location: tests/auth.setup.konza.ts:15:6

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /authenticator (code|verification)/i })
Expected: visible
Timeout: 30000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 30000ms
  - waiting for getByRole('heading', { name: /authenticator (code|verification)/i })

```

# Test source

```ts
  1  | import { Page, Locator, expect } from '@playwright/test';
  2  | 
  3  | export class TaifaSsoAuthenticatorPage {
  4  |   readonly page: Page;
  5  |   readonly codeInput: Locator;
  6  |   readonly verifyButton: Locator;
  7  | 
  8  |   constructor(page: Page) {
  9  |     this.page = page;
  10 |     this.codeInput = page.getByRole('textbox', { name: /verification code/i });
  11 |     // Matches both "Verify & Sign In" and the current "Verify & Continue" —
  12 |     // the live UI was found to use the latter (see RMSK ticket pending),
  13 |     // kept lenient so a future copy change doesn't reintroduce the mismatch.
  14 |     this.verifyButton = page.getByRole('button', { name: /verify\s*&\s*(sign\s*in|continue)/i });
  15 |   }
  16 | 
  17 |   async assertPageLoaded() {
  18 |     // Matches both "Authenticator Code" and the current "Authenticator
  19 |     // Verification" heading text.
> 20 |     await expect(this.page.getByRole('heading', { name: /authenticator (code|verification)/i })).toBeVisible({ timeout: 30000 });
     |                                                                                                  ^ Error: expect(locator).toBeVisible() failed
  21 |     await expect(this.codeInput).toBeVisible();
  22 |   }
  23 | 
  24 |   async enterCode(code: string) {
  25 |     await this.codeInput.fill(code);
  26 |     await this.verifyButton.click();
  27 |   }
  28 | }
  29 | 
```