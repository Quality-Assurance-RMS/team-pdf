# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/Dashboard/dash-rc-tc6-revenue-chart-summary-totals.spec.ts >> Dashboard — Monthly Revenue Chart >> TC6 — Revenue summary panel shows Gross Amount, Net Amount, Commission and Transaction Success Ratio
- Location: tests/UI/Dashboard/dash-rc-tc6-revenue-chart-summary-totals.spec.ts:31:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/Gross Amount/i)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/Gross Amount/i)

```

```yaml
- img "Unified Control"
- paragraph: Government services, now at your fingertips
- img
- paragraph: RMS
- paragraph: National Treasury
- heading "Login" [level=1]
- paragraph: Access Revenue Management System (RMS)
- text: Email address
- textbox "Enter Your email" [disabled]
- paragraph
- text: Password
- textbox "Enter Password" [disabled]
- button [disabled]
- paragraph
- checkbox "Remember for 30 days" [disabled]
- text: Remember for 30 days
- link "Forgot Password?":
  - /url: /auth/forgot-password
- button "Log In →" [disabled]
- text: Or
- button "Login with SSO →"
- region "Notifications alt+T"
```

# Test source

```ts
  1  | /**
  2  |  * @file revenue-chart-summary-totals.spec.ts
  3  |  * @testId DASH-RC-TC6
  4  |  * @feature Dashboard — Monthly Revenue Chart
  5  |  * @priority P1 / Critical
  6  |  *
  7  |  * PURPOSE
  8  |  * -------
  9  |  * Verifies that the summary panel displayed inside the Monthly Revenue chart
  10 |  * renders all four key metric labels:
  11 |  *   1. Gross Amount             — cumulative gross revenue for the year
  12 |  *   2. Net Amount               — net revenue after fees
  13 |  *   3. Commission               — total platform fees collected
  14 |  *   4. Transaction Success Ratio — % of initiated payments that completed
  15 |  *
  16 |  * PRE-CONDITIONS
  17 |  * --------------
  18 |  *  - Authenticated session (OTP login)
  19 |  *  - Revenue chart section mounted (TC1 must pass)
  20 |  */
  21 | 
  22 | import { test, expect } from '../fixtures';
  23 | import type { TestInfo } from '@playwright/test';
  24 | import { allure } from 'allure-playwright';
  25 | import { DashboardPage } from '../../pages/DashboardPage';
  26 | import { attachScreenshot } from '../../utils/screenshotHelper';
  27 | 
  28 | test.describe('Dashboard — Monthly Revenue Chart', () => {
  29 |   test.setTimeout(120000);
  30 | 
  31 |   test('TC6 — Revenue summary panel shows Gross Amount, Net Amount, Commission and Transaction Success Ratio', async ({ page, logger }, testInfo: TestInfo) => {
  32 |     await allure.description('Verifies that the chart summary panel shows all four summary labels.');
  33 |     await allure.label('feature',  'Dashboard');
  34 |     await allure.label('story',    'Revenue Chart');
  35 |     await allure.label('severity', 'critical');
  36 |     await allure.label('priority', 'P1');
  37 |     await allure.label('testId',   'DASH-RC-TC6');
  38 | 
  39 |     await logger.step('Step 1 — Verifies that the chart summary panel shows all four summary labels', async () => {
  40 |       logger.info('Test scope: Verifies that the chart summary panel shows all four summary labels');
  41 |     });
  42 | 
  43 |     const dashboard = new DashboardPage(page);
  44 | 
  45 |     await logger.step('Step 2 — Navigate to Dashboard page', async () => {
  46 |       logger.info('Opening the Dashboard page');
  47 |       await dashboard.goto();
  48 |       logger.pass('Navigation to Dashboard initiated successfully');
  49 |       await attachScreenshot(page, testInfo, '01 — Page loading');
  50 |     });
  51 | 
  52 |     await logger.step('Step 3 — Verify page loads successfully', async () => {
  53 |       logger.info('Waiting for all Dashboard elements to render');
  54 |       await dashboard.assertPageLoaded();
  55 |       logger.pass('Dashboard page is fully loaded with all required elements');
  56 |       await attachScreenshot(page, testInfo, '02 — Page loaded and ready');
  57 |     });
  58 | 
  59 |     await logger.step('Step 4 — "Gross Amount" label is visible', async () => {
  60 |       logger.info('Asserting visibility: Gross Amount summary label');
> 61 |       await expect(page.getByText(/Gross Amount/i)).toBeVisible();
     |                                                     ^ Error: expect(locator).toBeVisible() failed
  62 |       logger.pass('Gross Amount label is visible');
  63 |       await attachScreenshot(page, testInfo, '03 — Gross Amount label visible', page.getByText(/Gross Amount/i));
  64 |     });
  65 | 
  66 |     await logger.step('Step 5 — "Net Amount" label is visible', async () => {
  67 |       logger.info('Asserting visibility: Net Amount summary label');
  68 |       await expect(page.getByText(/Net Amount/i)).toBeVisible();
  69 |       logger.pass('Net Amount label is visible');
  70 |       await attachScreenshot(page, testInfo, '04 — Net Amount label visible', page.getByText(/Net Amount/i));
  71 |     });
  72 | 
  73 |     await logger.step('Step 6 — "Commission" label is visible', async () => {
  74 |       logger.info('Asserting visibility: Commission summary label');
  75 |       await expect(page.getByText(/^Commission$/i)).toBeVisible();
  76 |       logger.pass('Commission label is visible');
  77 |       await attachScreenshot(page, testInfo, '05 — Commission label visible', page.getByText(/^Commission$/i));
  78 |     });
  79 | 
  80 |     await logger.step('Step 7 — "Transaction Success Ratio" label is visible', async () => {
  81 |       logger.info('Asserting visibility: Transaction Success Ratio summary label');
  82 |       await expect(page.getByText(/Transaction Success Ratio/i)).toBeVisible();
  83 |       logger.pass('Transaction Success Ratio label is visible');
  84 |       await attachScreenshot(page, testInfo, '06 — Transaction Success Ratio label visible', page.getByText(/Transaction Success Ratio/i));
  85 |     });
  86 |   });
  87 | });
  88 | 
```