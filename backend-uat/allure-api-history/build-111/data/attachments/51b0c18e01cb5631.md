# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-transaction-api.spec.ts >> Transaction API — Full Coverage >> TC-TXN-008 — Filter Options: Retrieve available filter values
- Location: tests/API/backend-tests/backend-transaction-api.spec.ts:766:7

# Error details

```
Error: statuses must not be empty

expect(received).toBeGreaterThan(expected)

Expected: > 0
Received:   0
```

# Test source

```ts
  705 |           allure.parameter('payment_reference',          String(body.payment_reference));
  706 |           allure.parameter('invoice_id',                 String(body.invoice_id));
  707 |         });
  708 | 
  709 |         await logger.step('Step 5 — Validate payment fields', async () => {
  710 |           const body = res!.body;
  711 |           logger.info('Asserting: payment_method, gateway_status, gateway_name, amounts present and valid');
  712 |           expect(body.payment_method, 'payment_method must be present').toBeTruthy();
  713 |           expect(body.gateway_status, 'gateway_status must be present').toBeTruthy();
  714 |           expect(body.gateway_name,   'gateway_name must be present').toBeTruthy();
  715 |           expect(typeof body.pg_amount_charged,       'pg_amount_charged must be a number').toBe('number');
  716 |           expect(typeof body.net_settlement_amount,   'net_settlement_amount must be a number').toBe('number');
  717 |           expect(body.pg_amount_charged,  'pg_amount_charged must be positive').toBeGreaterThan(0);
  718 |           logger.pass('All assertions passed');
  719 |           allure.parameter('payment_method',          String(body.payment_method));
  720 |           allure.parameter('gateway_status',          String(body.gateway_status));
  721 |           allure.parameter('gateway_name',            String(body.gateway_name));
  722 |           allure.parameter('pg_amount_charged',       String(body.pg_amount_charged));
  723 |           allure.parameter('net_settlement_amount',   String(body.net_settlement_amount));
  724 |         });
  725 | 
  726 |         await logger.step('Step 6 — Validate service metadata fields', async () => {
  727 |           const body = res!.body;
  728 |           logger.info('Asserting: mcda_name, service_name, trnx_date present');
  729 |           expect(body.mcda_name,    'mcda_name must be present').toBeTruthy();
  730 |           expect(body.service_name, 'service_name must be present').toBeTruthy();
  731 |           expect(body.trnx_date,    'trnx_date must be present').toBeTruthy();
  732 |           logger.pass('All assertions passed');
  733 |           allure.parameter('mcda_name',    String(body.mcda_name));
  734 |           allure.parameter('service_name', String(body.service_name));
  735 |           allure.parameter('trnx_date',    String(body.trnx_date));
  736 |         });
  737 | 
  738 |         await logger.step('Step 7 — Validate payer information fields', async () => {
  739 |           const body = res!.body;
  740 |           logger.info('Asserting: payer_name, payer_phone, payer_email present');
  741 |           expect(body.payer_name,  'payer_name must be present').toBeTruthy();
  742 |           expect(body.payer_phone, 'payer_phone must be present').toBeTruthy();
  743 |           expect(body.payer_email, 'payer_email must be present').toBeTruthy();
  744 |           logger.pass('All assertions passed');
  745 |           allure.parameter('payer_name',  String(body.payer_name));
  746 |           allure.parameter('payer_phone', String(body.payer_phone));
  747 |           allure.parameter('payer_email', String(body.payer_email));
  748 |         });
  749 | 
  750 |         await logger.step('Step 8 — Validate trnx_date is a valid date string', async () => {
  751 |           const dateStr = res!.body.trnx_date as string;
  752 |           const dateRegex = /^\d{4}-\d{2}-\d{2}$/;
  753 |           logger.info('Asserting: trnx_date matches YYYY-MM-DD format');
  754 |           expect(dateRegex.test(dateStr), `trnx_date "${dateStr}" must match YYYY-MM-DD format`).toBe(true);
  755 |           logger.pass('All assertions passed');
  756 |           allure.parameter('trnx_date value',  dateStr);
  757 |           allure.parameter('trnx_date format', 'YYYY-MM-DD — PASS');
  758 |         });
  759 |       });
  760 |     }
  761 |   });
  762 | 
  763 |   // ===========================================================================
  764 |   // TC-TXN-008 — Filter Options
  765 |   // ===========================================================================
  766 |   test('TC-TXN-008 — Filter Options: Retrieve available filter values', async ({ logger }) => {
  767 | 
  768 |     setCommonLabels('TC-TXN-008', 'Filter Options', 'normal');
  769 |     await allure.description(
  770 |       'Verifies the GET /transaction/filter-options endpoint.\n\n' +
  771 |       '**Endpoint:** GET /api/v1/transaction/filter-options\n' +
  772 |       '**Expected:** HTTP 200 with arrays of statuses (SUCCESS, FAILED, PENDING), ' +
  773 |       'methods (MPESA, CARD, BANK), txn_types, and categories.',
  774 |     );
  775 | 
  776 |     let res: supertest.Response;
  777 | 
  778 |     await logger.step('Step 1 — Build request parameters', async () => {
  779 |       allure.parameter('Base URL',    BASE_URL);
  780 |       allure.parameter('Endpoint',    '/api/v1/transaction/filter-options');
  781 |       allure.parameter('Method',      'GET');
  782 |       allure.parameter('Auth',        'Bearer token present: ' + (BEARER_TOKEN ? 'YES' : 'NO'));
  783 |     });
  784 | 
  785 |     await logger.step('Step 2 — Send GET /transaction/filter-options', async () => {
  786 |       logger.info('GET /api/v1/transaction/filter-options');
  787 |       res = await apiGet('/api/v1/transaction/filter-options');
  788 |       logger.pass('HTTP ' + res.status + ' received');
  789 |       attachRequestResponse('GET', '/api/v1/transaction/filter-options', {}, res);
  790 |       allure.parameter('HTTP Status',  String(res.status));
  791 |       allure.parameter('Content-Type', String(res.headers['content-type'] ?? 'N/A'));
  792 |     });
  793 | 
  794 |     await logger.step('Step 3 — Validate HTTP 200 status code', async () => {
  795 |       logger.info('Asserting: HTTP 200, body defined, body is object');
  796 |       assertOk(res!, 'TC-TXN-008');
  797 |       logger.pass('All assertions passed');
  798 |       allure.parameter('Status Assertion', 'PASS — HTTP 200 received');
  799 |     });
  800 | 
  801 |     await logger.step('Step 4 — Validate statuses array is present and non-empty', async () => {
  802 |       const statuses: string[] = res!.body.statuses ?? [];
  803 |       logger.info('Asserting: statuses is a non-empty array');
  804 |       expect(Array.isArray(statuses), 'statuses must be an array').toBe(true);
> 805 |       expect(statuses.length,         'statuses must not be empty').toBeGreaterThan(0);
      |                                                                     ^ Error: statuses must not be empty
  806 |       logger.pass('All assertions passed');
  807 |       allure.parameter('statuses count',  String(statuses.length));
  808 |       allure.parameter('statuses values', statuses.join(', '));
  809 |     });
  810 | 
  811 |     await logger.step('Step 5 — Validate known statuses are present', async () => {
  812 |       const statuses: string[] = res!.body.statuses ?? [];
  813 |       const knownStatuses = ['SUCCESS', 'FAILED', 'PENDING'];
  814 |       logger.info('Asserting: SUCCESS, FAILED, PENDING all present in statuses array');
  815 |       for (const s of knownStatuses) {
  816 |         expect(statuses, `"${s}" must be in statuses array`).toContain(s);
  817 |       }
  818 |       logger.pass('All assertions passed');
  819 |       allure.parameter('Expected statuses', knownStatuses.join(', '));
  820 |       allure.parameter('Validation',        'All expected statuses found — PASS');
  821 |     });
  822 | 
  823 |     await logger.step('Step 6 — Validate methods array is present', async () => {
  824 |       const methods: string[] = res!.body.methods ?? [];
  825 |       logger.info('Asserting: methods is a non-empty array');
  826 |       expect(Array.isArray(methods), 'methods must be an array').toBe(true);
  827 |       expect(methods.length,         'methods must not be empty').toBeGreaterThan(0);
  828 |       logger.pass('All assertions passed');
  829 |       allure.parameter('methods count',  String(methods.length));
  830 |       allure.parameter('methods values', methods.join(', '));
  831 |     });
  832 | 
  833 |     await logger.step('Step 7 — Validate known payment methods are present', async () => {
  834 |       const methods: string[] = res!.body.methods ?? [];
  835 |       const knownMethods = ['MPESA', 'CARD', 'BANK'];
  836 |       logger.info('Asserting: MPESA, CARD, BANK all present in methods array');
  837 |       for (const m of knownMethods) {
  838 |         expect(methods, `"${m}" must be in methods array`).toContain(m);
  839 |       }
  840 |       logger.pass('All assertions passed');
  841 |       allure.parameter('Expected methods', knownMethods.join(', '));
  842 |       allure.parameter('Validation',       'All expected methods found — PASS');
  843 |     });
  844 | 
  845 |     await logger.step('Step 8 — Validate txn_types array is present', async () => {
  846 |       const types = res!.body.txn_types ?? [];
  847 |       logger.info('Asserting: txn_types is a non-empty array');
  848 |       expect(Array.isArray(types), 'txn_types must be an array').toBe(true);
  849 |       expect(types.length,         'txn_types must not be empty').toBeGreaterThan(0);
  850 |       logger.pass('All assertions passed');
  851 |       allure.parameter('txn_types count',  String(types.length));
  852 |       allure.parameter('txn_types values', JSON.stringify(types));
  853 |     });
  854 | 
  855 |     await logger.step('Step 9 — Validate categories array is present', async () => {
  856 |       const categories: string[] = res!.body.categories ?? [];
  857 |       logger.info('Asserting: categories is a non-empty array');
  858 |       expect(Array.isArray(categories), 'categories must be an array').toBe(true);
  859 |       expect(categories.length,         'categories must not be empty').toBeGreaterThan(0);
  860 |       logger.pass('All assertions passed');
  861 |       allure.parameter('categories count',  String(categories.length));
  862 |       allure.parameter('categories values', categories.join(', '));
  863 |     });
  864 |   });
  865 | 
  866 |   // ===========================================================================
  867 |   // TC-TXN-009 — Auth: 401 on missing / invalid token
  868 |   // ===========================================================================
  869 |   test('TC-TXN-009 — Security: Returns 401 when Authorization header is missing', async ({ logger }) => {
  870 | 
  871 |     setCommonLabels('TC-TXN-009', 'Authentication', 'critical');
  872 |     await allure.description(
  873 |       'Confirms the API enforces authentication and rejects unauthenticated requests.\n\n' +
  874 |       '**Endpoint:** GET /api/v1/transaction/summary?period=today\n' +
  875 |       '**Headers:** No Authorization header\n' +
  876 |       '**Expected:** HTTP 401 Unauthorized with an error message.\n\n' +
  877 |       'This validates that the API is properly secured and not publicly accessible.',
  878 |     );
  879 | 
  880 |     let res: supertest.Response;
  881 | 
  882 |     await logger.step('Step 1 — Build unauthenticated request', async () => {
  883 |       allure.parameter('Base URL',       BASE_URL);
  884 |       allure.parameter('Endpoint',       '/api/v1/transaction/summary?period=today');
  885 |       allure.parameter('Method',         'GET');
  886 |       allure.parameter('Authorization',  'NOT PROVIDED — intentionally omitted');
  887 |     });
  888 | 
  889 |     await logger.step('Step 2 — Send GET /summary without Authorization header', async () => {
  890 |       logger.info('GET /api/v1/transaction/summary?period=today (no auth)');
  891 |       res = await supertest(BASE_URL)
  892 |         .get('/api/v1/transaction/summary?period=today')
  893 |         .set('Accept', 'application/json');
  894 |       logger.pass('HTTP ' + res.status + ' received');
  895 | 
  896 |       console.log('[No-Auth GET /summary] status:', res.status, '| body:', JSON.stringify(res.body, null, 2));
  897 | 
  898 |       allure.attachment(
  899 |         'Unauthenticated Request & Response',
  900 |         JSON.stringify({
  901 |           request:  { method: 'GET', url: `${BASE_URL}/api/v1/transaction/summary?period=today`, headers: {} },
  902 |           response: { status: res.status, body: res.body },
  903 |         }, null, 2),
  904 |         'application/json',
  905 |       );
```