# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-monthly-revenue.spec.ts >> Backend (Konza) â€” Dashboard Monthly Revenue >> TC-DMREV-001 â€” Happy Path: 200 + year + months[12] + summary
- Location: tests/API/backend-tests/backend-dashboard-monthly-revenue.spec.ts:180:7

# Error details

```
Error: "data.year" must be present

expect(received).toHaveProperty(path)

Expected path: "year"
Received path: []

Received value: {"months": [{"commissionKes": 799969.64, "commissionUsd": 0, "grossKes": 5000716.89, "grossUsd": 0, "monthLabel": "Oct", "monthNum": 10, "netKes": 4200747.25, "netUsd": 0, "year": 2025}, {"commissionKes": 0, "commissionUsd": 0, "grossKes": 0, "grossUsd": 0, "monthLabel": "Nov", "monthNum": 11, "netKes": 0, "netUsd": 0, "year": 2025}, {"commissionKes": 21238.98, "commissionUsd": 0, "grossKes": 2123894.37, "grossUsd": 0, "monthLabel": "Dec", "monthNum": 12, "netKes": 2102655.39, "netUsd": 0, "year": 2025}, {"commissionKes": 57739.48, "commissionUsd": 0, "grossKes": 388099.6, "grossUsd": 0, "monthLabel": "Jan", "monthNum": 1, "netKes": 330360.12, "netUsd": 0, "year": 2026}, {"commissionKes": 240074.48, "commissionUsd": 0, "grossKes": 1500501.22, "grossUsd": 0, "monthLabel": "Feb", "monthNum": 2, "netKes": 1260426.74, "netUsd": 0, "year": 2026}, {"commissionKes": 319915.76, "commissionUsd": 0, "grossKes": 1998946.98, "grossUsd": 0, "monthLabel": "Mar", "monthNum": 3, "netKes": 1679031.22, "netUsd": 0, "year": 2026}, {"commissionKes": 319700.22, "commissionUsd": 0, "grossKes": 1998369.88, "grossUsd": 0, "monthLabel": "Apr", "monthNum": 4, "netKes": 1678669.66, "netUsd": 0, "year": 2026}, {"commissionKes": 2398134.05, "commissionUsd": 0, "grossKes": 14994819.86, "grossUsd": 0, "monthLabel": "May", "monthNum": 5, "netKes": 12596685.81, "netUsd": 0, "year": 2026}, {"commissionKes": 5104.26, "commissionUsd": 0, "grossKes": 35168.49, "grossUsd": 0, "monthLabel": "Jun", "monthNum": 6, "netKes": 30064.23, "netUsd": 0, "year": 2026}, {"commissionKes": 1233, "commissionUsd": 0, "grossKes": 8815638.96, "grossUsd": 768077.6, "monthLabel": "Jul", "monthNum": 7, "netKes": 8814405.96, "netUsd": 768077.6, "year": 2026}, …], "summary": {"conversionRatioKesPct": 100, "conversionRatioUsdPct": 100, "totalCommissionKes": 6019152.24, "totalCommissionUsd": 1439877.52, "totalGrossKes": 59984146.31, "totalGrossUsd": 11788077.58, "totalNetKes": 53964994.07, "totalNetUsd": 10348200.06}}
```

# Test source

```ts
  113 |   console.log(`  â–º HTTP Status           : ${res.status}`);
  114 |   console.log(`  â–º Content-Type         : ${res.headers['content-type'] ?? 'N/A'}`);
  115 |   if (d) {
  116 |     console.log(`  â–º year                 : ${d.year}`);
  117 |     console.log(`  â–º months count         : ${d.months?.length}`);
  118 |     const activeMonths = d.months?.filter(m => m.gross_kes > 0 || m.gross_usd > 0);
  119 |     console.log(`  â–º months with data     : ${activeMonths?.length} (${activeMonths?.map(m=>m.month_label).join(', ')})`);
  120 |     const s = d.summary;
  121 |     if (s) {
  122 |       console.log(`  â–º summary.total_gross_kes  : ${s.total_gross_kes?.toLocaleString()}`);
  123 |       console.log(`  â–º summary.total_gross_usd  : ${s.total_gross_usd?.toLocaleString()}`);
  124 |       console.log(`  â–º summary.total_net_kes    : ${s.total_net_kes?.toLocaleString()}`);
  125 |       console.log(`  â–º summary.total_net_usd    : ${s.total_net_usd?.toLocaleString()}`);
  126 |       console.log(`  â–º summary.total_commission_kes : ${s.total_commission_kes?.toLocaleString()}`);
  127 |       console.log(`  â–º summary.total_payments   : ${s.total_payments}`);
  128 |       console.log(`  â–º summary.conversion_ratio_pct: ${s.conversion_ratio_pct}`);
  129 |       // Cross-endpoint flag
  130 |       if (s.total_payments > 0) {
  131 |         const mixed = s.total_gross_kes + s.total_gross_usd;
  132 |         const diff  = Math.abs(REVENUE_TREND_TOTAL_KES - s.total_gross_kes);
  133 |         console.log(`  â–º /revenue-trend KES total : ${REVENUE_TREND_TOTAL_KES.toLocaleString()}`);
  134 |         console.log(`  â–º KES + USD (if mixed)     : ${mixed.toFixed(2)} â†’ matches trend? ${Math.abs(mixed - REVENUE_TREND_TOTAL_KES) < 1}`);
  135 |         if (Math.abs(mixed - REVENUE_TREND_TOTAL_KES) < 1) {
  136 |           console.warn(`  âš  BUG-MREV-04: /revenue-trend total equals KES+USD sum â†’ currency mixing confirmed`);
  137 |         }
  138 |       }
  139 |     }
  140 |   }
  141 |   console.log(`  â–º Cache-Control        : ${res.headers['cache-control'] ?? 'MISSING'}`);
  142 |   console.log(`${'â”€'.repeat(70)}\n`);
  143 | }
  144 | 
  145 | function flagIssue(tcId: string, issueId: string, description: string, actual: unknown) {
  146 |   const msg = `[${tcId}] ${issueId}: ${description} | Actual: ${JSON.stringify(actual)}`;
  147 |   console.warn(`âš  ISSUE FLAGGED â€” ${msg}`);
  148 |   allure.attachment(`âš  ${issueId}`, msg, 'text/plain');
  149 | }
  150 | 
  151 | function attachResponse(tcId: string, res: supertest.Response, extra: Record<string, unknown> = {}) {
  152 |   allure.attachment(
  153 |     `[${tcId}] Request & Response`,
  154 |     JSON.stringify({
  155 |       request:  { method: 'GET', url: `${BASE_URL}${ENDPOINT}`, headers: { Authorization: 'Bearer ***redacted***' } },
  156 |       response: { status: res.status, headers: res.headers, body: res.body, ...extra },
  157 |     }, null, 2),
  158 |     'application/json',
  159 |   );
  160 | }
  161 | 
  162 | function backendLabels(tcId: string, title: string, severity: 'blocker' | 'critical' | 'normal' | 'minor' = 'critical') {
  163 |   allure.parentSuite('RMS Dashboard API');
  164 |   allure.suite('Backend');
  165 |   allure.subSuite('Monthly Revenue Endpoint');
  166 |   allure.label('testId',   tcId);
  167 |   allure.label('severity', severity);
  168 |   allure.owner('Ashil Shaji');
  169 |   allure.tags('Backend', 'Konza', 'API', 'MonthlyRevenue');
  170 |   allure.label('story', title);
  171 | }
  172 | 
  173 | // â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
  174 | // Suite
  175 | // â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
  176 | 
  177 | test.describe('Backend (Konza) â€” Dashboard Monthly Revenue', () => {
  178 | 
  179 |   // â”€â”€ TC-DMREV-001 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  180 |   test('TC-DMREV-001 â€” Happy Path: 200 + year + months[12] + summary', async ({ logger }) => {
  181 |     backendLabels('TC-DMREV-001', 'Happy Path â€” Monthly Revenue');
  182 |     let res: supertest.Response;
  183 | 
  184 |     await logger.step('Step 1 â€” Build request', async () => {
  185 |       allure.parameter('Base URL',  BASE_URL);
  186 |       allure.parameter('Endpoint',  ENDPOINT);
  187 |       allure.parameter('Method',    'GET');
  188 |       allure.parameter('Auth',      `Bearer token present: ${BEARER_TOKEN ? 'YES' : 'NO'}`);
  189 |     });
  190 | 
  191 |     await logger.step('Step 2 â€” Send GET /monthly-revenue and log response', async () => {
  192 |       logger.info(`GET ${ENDPOINT}`);
  193 |       res = await get();
  194 |       logger.pass('HTTP ' + res.status + ' received');
  195 |       logResponse('TC-DMREV-001', res);
  196 |       attachResponse('TC-DMREV-001', res);
  197 |       allure.parameter('HTTP Status',  String(res.status));
  198 |       allure.parameter('Content-Type', String(res.headers['content-type'] ?? 'N/A'));
  199 |     });
  200 | 
  201 |     await logger.step('Step 3 â€” Validate HTTP 200 and Content-Type', async () => {
  202 |       logger.info('Asserting: Validate HTTP 200 and Content-Type');
  203 |       expect(res!.status, 'HTTP 200 expected').toBe(200);
  204 |       expect(res!.body,   'Body must be defined').toBeDefined();
  205 |       expect(res!.headers['content-type'], 'Content-Type must be application/json').toContain('application/json');
  206 |       logger.pass('All assertions passed');
  207 |     });
  208 | 
  209 |     await logger.step('Step 4 â€” Validate top-level data wrapper', async () => {
  210 |       logger.info('Asserting: Validate top-level data wrapper');
  211 |       expect(res!.body, 'Response must have "data" key').toHaveProperty('data');
  212 |       const d = res!.body.data as MonthlyRevenueData;
> 213 |       expect(d, '"data.year" must be present').toHaveProperty('year');
      |                                                ^ Error: "data.year" must be present
  214 |       expect(d, '"data.months" must be present').toHaveProperty('months');
  215 |       expect(d, '"data.summary" must be present').toHaveProperty('summary');
  216 |       allure.parameter('data.year', String(d.year));
  217 |       logger.pass('All assertions passed');
  218 |     });
  219 | 
  220 |     await logger.step('Step 5 â€” Validate year field is current year', async () => {
  221 |       const d = res!.body.data as MonthlyRevenueData;
  222 |       const currentYear = new Date().getFullYear();
  223 |       logger.info('Asserting: Validate year field is current year');
  224 |       expect(typeof d.year, 'year must be a number').toBe('number');
  225 |       expect(d.year, `year must equal current year (${currentYear})`).toBe(currentYear);
  226 |       allure.parameter('Returned year', String(d.year));
  227 |       logger.pass('All assertions passed');
  228 |     });
  229 | 
  230 |     await logger.step('Step 6 â€” Validate months array has exactly 12 entries (Janâ€“Dec)', async () => {
  231 |       const d = res!.body.data as MonthlyRevenueData;
  232 |       logger.info('Asserting: Validate months array has exactly 12 entries (Jan');
  233 |       expect(Array.isArray(d.months), '"months" must be an array').toBe(true);
  234 |       expect(d.months.length, '"months" must have exactly 12 entries').toBe(12);
  235 | 
  236 |       const monthNums = d.months.map(m => m.month_num);
  237 |       for (let i = 1; i <= 12; i++) {
  238 |         expect(monthNums, `month_num ${i} must be present`).toContain(i);
  239 |       }
  240 |       allure.parameter('Months count', String(d.months.length));
  241 |       allure.parameter('Month labels', d.months.map(m => m.month_label).join(', '));
  242 |       logger.pass('All assertions passed');
  243 |     });
  244 | 
  245 |     await logger.step('Step 7 â€” Validate each month has all 6 required fields', async () => {
  246 |       const d = res!.body.data as MonthlyRevenueData;
  247 |       const requiredFields = ['month_num','month_label','gross_kes','gross_usd','net_kes','net_usd','commission_kes','commission_usd'];
  248 |       for (const [i, month] of d.months.entries()) {
  249 |         for (const field of requiredFields) {
  250 |           logger.info('Asserting: Validate each month has all 6 required fields');
  251 |           expect(month, `months[${i}] must have "${field}"`).toHaveProperty(field);
  252 |         }
  253 |       }
  254 |       logger.pass('All assertions passed');
  255 |     });
  256 | 
  257 |     await logger.step('Step 8 â€” Validate summary has all required fields', async () => {
  258 |       const s = res!.body.data.summary as Summary;
  259 |       const requiredFields = ['total_payments','total_gross_kes','total_gross_usd','total_net_kes','total_net_usd','total_commission_kes','total_commission_usd','conversion_ratio_pct'];
  260 |       for (const field of requiredFields) {
  261 |         logger.info('Asserting: Validate summary has all required fields');
  262 |         expect(s, `summary must have "${field}"`).toHaveProperty(field);
  263 |         allure.parameter(`summary.${field}`, String((s as unknown as Record<string,unknown>)[field]));
  264 |       }
  265 |       logger.pass('All assertions passed');
  266 |     });
  267 |   });
  268 | 
  269 |   // â”€â”€ TC-DMREV-002 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  270 |   test('TC-DMREV-002 â€” Auth Guard: missing token â†’ 401, wrong token â†’ 401', async ({ logger }) => {
  271 |     backendLabels('TC-DMREV-002', 'Auth Guard', 'blocker');
  272 |     let resNoAuth: supertest.Response, resWrongAuth: supertest.Response;
  273 | 
  274 |     await logger.step('Step 1 â€” No Authorization header', async () => {
  275 |       logger.info(`GET ${ENDPOINT}`);
  276 |       resNoAuth = await supertest(BASE_URL).get(ENDPOINT).set('Accept', 'application/json');
  277 |       logger.pass('HTTP ' + resNoAuth.status + ' received');
  278 |       console.log(`[TC-DMREV-002] No-auth: ${resNoAuth.status} â€” ${resNoAuth.body?.message}`);
  279 |       allure.parameter('No-auth status',  String(resNoAuth.status));
  280 |       allure.parameter('No-auth message', String(resNoAuth.body?.message ?? 'N/A'));
  281 |     });
  282 | 
  283 |     await logger.step('Step 2 â€” Invalid Bearer token', async () => {
  284 |       logger.info(`GET ${ENDPOINT}`);
  285 |       resWrongAuth = await supertest(BASE_URL).get(ENDPOINT)
  286 |         .set('Authorization', 'Bearer invalidtoken.bad.sig').set('Accept', 'application/json');
  287 |       logger.pass('HTTP ' + resWrongAuth.status + ' received');
  288 |       console.log(`[TC-DMREV-002] Wrong-auth: ${resWrongAuth.status} â€” ${resWrongAuth.body?.message}`);
  289 |       allure.parameter('Wrong-auth status',  String(resWrongAuth.status));
  290 |       allure.parameter('Wrong-auth message', String(resWrongAuth.body?.message ?? 'N/A'));
  291 |     });
  292 | 
  293 |     await logger.step('Step 3 â€” Both must return 401 with correct messages', async () => {
  294 |       logger.info('Asserting: response structure and values');
  295 |       expect(resNoAuth!.status,    'Missing token must return 401').toBe(401);
  296 |       expect(resWrongAuth!.status, 'Invalid token must return 401').toBe(401);
  297 |       expect(resNoAuth!.body.status,    'No-auth body.status must be 401').toBe(401);
  298 |       expect(resWrongAuth!.body.status, 'Wrong-auth body.status must be 401').toBe(401);
  299 |       expect(resNoAuth!.body.message,    'No-auth message').toBe('Missing Authorization token');
  300 |       expect(resWrongAuth!.body.message, 'Wrong-auth message').toBe('Invalid or expired token');
  301 |       logger.pass('All assertions passed');
  302 |     });
  303 |   });
  304 | 
  305 |   // â”€â”€ TC-DMREV-003 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  306 |   test('TC-DMREV-003 â€” year param: valid values return correct year in response', async ({ logger }) => {
  307 |     backendLabels('TC-DMREV-003', 'Valid year Param Filtering', 'normal');
  308 | 
  309 |     await logger.step('Step 1 â€” year=2026 returns 2026 data', async () => {
  310 |       logger.info(`GET ${ENDPOINT}`);
  311 |       const res = await get('year=2026');
  312 |       logger.pass('HTTP ' + res.status + ' received');
  313 |       logger.info('Asserting: response structure and values');
```