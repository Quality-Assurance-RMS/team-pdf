# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/verification.spec.ts >> RMS OTP Verification >> should complete full login with OTP and reach dashboard
- Location: tests/UI/verification.spec.ts:17:7

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/auth\/verification/
Received string: "https://api.rms.dev.demo-fsit.com/auth/verification"
Timeout: 15000ms

Call log:
  - Expect "not toHaveURL" with timeout 15000ms
    33 × unexpected value "https://api.rms.dev.demo-fsit.com/auth/verification"

```

```yaml
- img "Unified Control"
- paragraph: Government services, now at your fingertips
- img
- heading "RMS" [level=1]
- text: National Treasury
- heading "Verification" [level=1]
- paragraph: Please enter the verification code sent to your registered Email ID
- paragraph: ashil.shaji@fortunesoftit.com
- textbox
- textbox
- textbox
- textbox
- text: "OTP expires in: 02:26 Didn't Receive OTP?"
- button "Resend"
- link "Back to Login":
  - /url: /auth/login
- region "Notifications alt+T"
```

# Test source

```ts
  1  | ﻿import { test, expect } from './fixtures';
  2  | import { LoginPage } from '../pages/LoginPage';
  3  | import { VerificationPage } from '../pages/VerificationPage';
  4  | import { fetchOtpFromEmail } from '../utils/graphMailHelper';
  5  | 
  6  | const EMAIL = process.env.LOGIN_EMAIL!;
  7  | const PASSWORD = process.env.LOGIN_PASSWORD!;
  8  | 
  9  | test.describe('RMS OTP Verification', () => {
  10 |   // These tests exercise the raw OTP verification flow starting from the login
  11 |   // page, so the global storageState must be cleared to prevent an automatic
  12 |   // redirect to /dashboard before the login form is even shown.
  13 |   test.use({ storageState: { cookies: [], origins: [] } });
  14 | 
  15 |   test.setTimeout(120000); // Allow time for email delivery (up to 90s polling)
  16 | 
  17 |   test('should complete full login with OTP and reach dashboard', async ({ page }) => {
  18 |     // Step 1: Record time just before login so we only fetch the fresh OTP
  19 |     const loginTime = new Date();
  20 | 
  21 |     const loginPage = new LoginPage(page);
  22 |     await loginPage.goto();
  23 |     await loginPage.login(EMAIL, PASSWORD);
  24 | 
  25 |     // Step 2: Should redirect to the OTP verification page
  26 |     const verificationPage = new VerificationPage(page);
  27 |     await verificationPage.assertPageLoaded();
  28 |     await expect(verificationPage.resendLink).toBeVisible();
  29 |     await expect(verificationPage.backToLoginLink).toBeVisible();
  30 | 
  31 |     // Step 3: Fetch only the OTP sent after loginTime (ignores stale previous OTPs)
  32 |     const otp = await fetchOtpFromEmail(EMAIL, process.env.OTP_SENDER_EMAIL!, loginTime);
  33 |     expect(otp).toMatch(/^\d{4}$/);
  34 | 
  35 |     // Step 4: Enter the OTP in the 4-box UI
  36 |     await verificationPage.enterOtp(otp);
  37 | 
  38 |     // Step 5: After correct OTP the app should navigate away from verification
> 39 |     await expect(page).not.toHaveURL(/\/auth\/verification/, { timeout: 15000 });
     |                            ^ Error: expect(page).not.toHaveURL(expected) failed
  40 | 
  41 |     // Step 6: Wait 10 seconds on the dashboard before test ends
  42 |     await page.waitForTimeout(10000);
  43 |   });
  44 | 
  45 |   test('should show error when wrong OTP is entered', async ({ page }) => {
  46 |     const loginPage = new LoginPage(page);
  47 |     await loginPage.goto();
  48 |     await loginPage.login(EMAIL, PASSWORD);
  49 | 
  50 |     const verificationPage = new VerificationPage(page);
  51 |     await verificationPage.assertPageLoaded();
  52 | 
  53 |     // Enter a deliberately wrong OTP
  54 |     await verificationPage.enterOtp('0000');
  55 | 
  56 |     // Should remain on verification page with an error
  57 |     await expect(page).toHaveURL(/\/auth\/verification/, { timeout: 10000 });
  58 |   });
  59 | 
  60 |   test('should allow resending OTP', async ({ page }) => {
  61 |     const loginPage = new LoginPage(page);
  62 |     await loginPage.goto();
  63 |     await loginPage.login(EMAIL, PASSWORD);
  64 | 
  65 |     const verificationPage = new VerificationPage(page);
  66 |     await verificationPage.assertPageLoaded();
  67 | 
  68 |     await verificationPage.clickResend();
  69 |     // Resend should keep us on verification page (new code dispatched)
  70 |     await expect(page).toHaveURL(/\/auth\/verification/);
  71 |   });
  72 | 
  73 |   test('should navigate back to login from verification page', async ({ page }) => {
  74 |     const loginPage = new LoginPage(page);
  75 |     await loginPage.goto();
  76 |     await loginPage.login(EMAIL, PASSWORD);
  77 | 
  78 |     const verificationPage = new VerificationPage(page);
  79 |     await verificationPage.assertPageLoaded();
  80 | 
  81 |     await verificationPage.clickBackToLogin();
  82 |     await expect(page).toHaveURL(/\/auth\/login/, { timeout: 8000 });
  83 |   });
  84 | });
  85 | 
```