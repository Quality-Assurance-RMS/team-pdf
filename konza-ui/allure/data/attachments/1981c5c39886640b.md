# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/Dashboard/dash-gp-tc3-gateway-payments-success-rate.spec.ts >> Dashboard — Gateway Payments >> TC3 — Success Rate metric shows a valid percentage (0–100%)
- Location: tests/UI/Dashboard/dash-gp-tc3-gateway-payments-success-rate.spec.ts:36:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/Success Rate/i).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/Success Rate/i).first()

```

```yaml
- complementary:
  - img "eCitizen"
  - navigation:
    - link "Dashboard":
      - /url: /dashboard
      - img
      - text: Dashboard
    - link "Transactions":
      - /url: /transaction
      - img
      - text: Transactions
    - link "Reconciliation":
      - /url: /reconciliation
      - img
      - text: Reconciliation
    - button "Expand Reconciliation":
      - img
    - link "Settlements":
      - /url: /settlements
      - img
      - text: Settlements
    - link "Payouts":
      - /url: /payouts
      - img
      - text: Payouts
    - link "SLA":
      - /url: /service-level-agreement/list
      - img
      - text: SLA
    - link "Reports":
      - /url: /reports
      - img
      - text: Reports
    - link "Administration":
      - /url: /psp-files
      - img
      - text: Administration
    - button "Expand Administration":
      - img
  - text: Awaiting Settlement
  - paragraph: Awaiting Settlement
  - button:
    - img
  - button:
    - img
  - text: Refresh status
  - button "Logout":
    - img
    - text: Logout
  - button "Collapse sidebar":
    - img
- banner:
  - button "Notifications"
  - text: A Ashil SUPER_ADMIN PLATFORM
- main:
  - heading "Hi, Ashil" [level=2]
  - paragraph: Here's what's happening with eCitizen Last 7 Days.
  - combobox:
    - option "Today"
    - option "Last 7 Days" [selected]
    - option "Last 30 Days"
    - option "Last 90 Days"
    - option "Custom Range"
- region "Notifications alt+T"
```

# Test source

```ts
  1   | /**
  2   |  * @file gateway-payments-success-rate.spec.ts
  3   |  * @testId DASH-GP-TC3
  4   |  * @feature Dashboard — Gateway Payments
  5   |  * @priority P0 / Critical
  6   |  *
  7   |  * PURPOSE
  8   |  * -------
  9   |  * Verifies that the "Success Rate" metric in the Gateway Payments section
  10  |  * is visible and shows a valid percentage between 0 and 100. This is a
  11  |  * critical SLA metric — it represents the percentage of gateway-processed
  12  |  * payments that completed successfully today.
  13  |  *
  14  |  * VALIDATION RULES
  15  |  * ----------------
  16  |  *  - Value must be a number followed by "%" (e.g. "100%", "91.6%", "0%")
  17  |  *  - Value must be ≥ 0 and ≤ 100
  18  |  *  - A value outside this range indicates a backend calculation error
  19  |  *
  20  |  * PRE-CONDITIONS
  21  |  * --------------
  22  |  *  - Authenticated session (OTP login)
  23  |  *  - Gateway Payments section mounted (TC1 must pass)
  24  |  *  - At least one gateway transaction exists for today
  25  |  */
  26  | 
  27  | import { test, expect } from '../fixtures';
  28  | import type { TestInfo } from '@playwright/test';
  29  | import { allure } from 'allure-playwright';
  30  | import { DashboardPage } from '../../pages/DashboardPage';
  31  | import { attachScreenshot } from '../../utils/screenshotHelper';
  32  | 
  33  | test.describe('Dashboard — Gateway Payments', () => {
  34  |   test.setTimeout(120000);
  35  | 
  36  |   test('TC3 — Success Rate metric shows a valid percentage (0–100%)', async ({ page, logger }, testInfo: TestInfo) => {
  37  |     // ── Allure metadata ──────────────────────────────────────────────────────
  38  |     await allure.description('Verifies the Success Rate in the Gateway Payments section is a valid percentage.');
  39  |     await allure.label('feature',  'Dashboard');
  40  |     await allure.label('story',    'Gateway Payments');
  41  |     await allure.label('severity', 'critical');
  42  |     await allure.label('priority', 'P0');
  43  |     await allure.label('testId',   'DASH-GP-TC3');
  44  | 
  45  |     await logger.step('Step 1 — Verifies the Success Rate in the Gateway Payments section is a valid percentage', async () => {
  46  |       logger.info('Test scope: Verifies the Success Rate in the Gateway Payments section is a valid percentage');
  47  |     });
  48  | 
  49  |     // ── Setup ────────────────────────────────────────────────────────────────
  50  |     const dashboard = new DashboardPage(page);
  51  | 
  52  |     await logger.step('Step 2 — Navigate to Dashboard page', async () => {
  53  |       logger.info('Opening the Dashboard page');
  54  |       await dashboard.goto();
  55  |       logger.pass('Navigation to Dashboard initiated successfully');
  56  |       await attachScreenshot(page, testInfo, '01 — Page loading');
  57  |     });
  58  | 
  59  |     await logger.step('Step 3 — Verify page loads successfully', async () => {
  60  |       logger.info('Waiting for all Dashboard elements to render');
  61  |       await dashboard.assertPageLoaded();
  62  |       logger.pass('Dashboard page is fully loaded with all required elements');
  63  |       await attachScreenshot(page, testInfo, '02 — Page loaded and ready');
  64  |     });
  65  | 
  66  |     // ── Assertions ───────────────────────────────────────────────────────────
  67  |     await logger.step('Step 4 — "Success Rate" label is visible', async () => {
  68  |       // .first() is used because "Success Rate" may also appear in chart legends
  69  |       logger.info('Asserting visibility: Success Rate metric label');
> 70  |       await expect(dashboard.successRateMetric).toBeVisible();
      |                                                 ^ Error: expect(locator).toBeVisible() failed
  71  |       logger.pass('Success Rate metric label is visible');
  72  |       await attachScreenshot(page, testInfo, '03 — Success Rate label visible', dashboard.successRateMetric);
  73  |     });
  74  | 
  75  |     await logger.step('Step 5 — Success Rate value is between 0 and 100', async () => {
  76  |       // Walk one DOM level up from the label to capture the full metric block,
  77  |       // which includes both the label and its numeric value
  78  |       logger.info('Reading Success Rate percentage value from the page');
  79  |       const el   = page.getByText(/Success Rate/i).first();
  80  |       const text = await el.locator('xpath=ancestor::*[1]').innerText()
  81  |         .catch(() => el.innerText());
  82  | 
  83  |       // Extract the first percentage pattern in the text block
  84  |       const match = text.match(/(\d+(\.\d+)?)%/);
  85  |       expect(match, 'Should contain a % value').not.toBeNull();
  86  | 
  87  |       const value = parseFloat(match![1]);
  88  | 
  89  |       // Attach the extracted value for easy debugging in Allure
  90  |       allure.parameter('Success Rate %', String(value));
  91  | 
  92  |       // A valid rate must lie within the mathematical bounds of 0–100%
  93  |       expect(value).toBeGreaterThanOrEqual(0);
  94  |       expect(value).toBeLessThanOrEqual(100);
  95  |       logger.pass('Value assertion passed');
  96  |       await attachScreenshot(page, testInfo, '04 — Success Rate value validated');
  97  |     });
  98  |   });
  99  | });
  100 | 
```