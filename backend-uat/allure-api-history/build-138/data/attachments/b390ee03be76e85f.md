# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-status-currency.spec.ts >> Backend (Konza) â€" Dashboard Status & Currency >> TC-DSH-005 â€" byMethod: MPESA + CARD entries with all four amount/count fields
- Location: tests/API/backend-tests/backend-dashboard-status-currency.spec.ts:533:7

# Error details

```
TypeError: fetch failed
```

# Test source

```ts
  1  | // Konza UAT presents a self-signed certificate (unlike the AWS environment's
  2  | // CA-signed cert), which Node rejects by default. This module is only ever
  3  | // imported by tests/API/backend-tests/*.spec.ts, so scope is limited to the
  4  | // Konza suite — run it via `npm run test:konza-backend` rather than folded
  5  | // into a combined `npm test` run, to avoid this leaking into AWS-facing
  6  | // worker processes.
  7  | process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0';
  8  | 
  9  | let _cachedToken: string | null = null;
  10 | 
  11 | // Uses the same test-only token endpoint AWS dev-demo exposes (see
  12 | // tests/utils/tokenHelper.ts) — email only, no password, always fetched
  13 | // fresh rather than pulled from a static env var. The previous approach
  14 | // (a static KONZA_BEARER_TOKEN checked first, falling back to a
  15 | // password-based /api/v1/auth/login call) left every backend test silently
  16 | // running against a stale token whenever KONZA_BEARER_TOKEN went stale in
  17 | // .env, since nothing validated its expiry before use.
  18 | export async function getKonzaToken(): Promise<string> {
  19 |   if (_cachedToken) return _cachedToken;
  20 | 
  21 |   const baseURL = process.env.KONZA_BASE_URL ?? 'https://rms.uat.konza';
  22 |   const email   = process.env.KONZA_LOGIN_EMAIL ?? '';
  23 | 
> 24 |   const res = await fetch(`${baseURL}/api/v1/auth/test/token`, {
     |               ^ TypeError: fetch failed
  25 |     method:  'POST',
  26 |     headers: { 'Content-Type': 'application/json' },
  27 |     body:    JSON.stringify({ email }),
  28 |   });
  29 | 
  30 |   if (!res.ok) throw new Error(`Konza token fetch failed: HTTP ${res.status}`);
  31 | 
  32 |   const data = await res.json() as { accessToken: string };
  33 |   _cachedToken = data.accessToken;
  34 |   return _cachedToken;
  35 | }
  36 | 
```