# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-revenue-trend.spec.ts >> Backend (Konza) â€” Dashboard Revenue Trend >> TC-DREVT-007 â€” BUG-REVT-06: isPostive for "Failed" must not be false when change is "+0%"
- Location: tests/API/backend-tests/backend-dashboard-revenue-trend.spec.ts:593:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 503
```

# Test source

```ts
  507 |       expect(labels.length, 'labels must have 12 or 13 entries for a monthly trend').toBeGreaterThanOrEqual(12);
  508 |       expect(labels.length, 'labels must not exceed 13 entries for a monthly trend').toBeLessThanOrEqual(13);
  509 |       logger.pass('All assertions passed');
  510 |     });
  511 |   });
  512 | 
  513 |   // â”€â”€ TC-DREVT-006 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  514 |   test('TC-DREVT-006 â€” BUG-REVT-05: change field must reflect real period-over-period delta, not always "+0%"', async ({ logger }) => {
  515 |     backendLabels('TC-DREVT-006', 'BUG-REVT-05 â€” change Always "+0%"', 'critical');
  516 | 
  517 |     let res: supertest.Response;
  518 | 
  519 |     await logger.step('Step 1 â€” Fetch revenue-trend response', async () => {
  520 |       logger.info(`GET ${ENDPOINT}`);
  521 |       res = await get();
  522 |       logger.pass('HTTP ' + res.status + ' received');
  523 |       logResponse('TC-DREVT-006', res, '(change field check)');
  524 |       attachResponse('TC-DREVT-006', res, { bug: 'BUG-REVT-05' });
  525 |     });
  526 | 
  527 |     await logger.step('Step 2 â€” Validate HTTP 200', async () => {
  528 |       logger.info('Asserting: Validate HTTP 200');
  529 |       expect(res!.status).toBe(200);
  530 |       logger.pass('All assertions passed');
  531 |     });
  532 | 
  533 |     await logger.step('Step 3 â€” BUG-REVT-05: change must not be "+0%" for every single metric', async () => {
  534 |       const summary = (res!.body.data as RevenueTrendData).summary;
  535 | 
  536 |       const allZeroChange = summary.every(s => s.change === '+0%');
  537 |       allure.parameter('All change values', summary.map(s => `${s.label}:${s.change}`).join(', '));
  538 |       allure.parameter('All changes are "+0%"', String(allZeroChange));
  539 | 
  540 |       if (allZeroChange) {
  541 |         flagIssue('TC-DREVT-006', 'BUG-REVT-05',
  542 |           'Every summary item has change="+0%" â€” this is almost certainly a calculation bug. ' +
  543 |           'With 23 929 total transactions visible in the trend, the change from a prior period cannot ' +
  544 |           'realistically be +0% for all three metrics simultaneously. ' +
  545 |           'Possible cause: no prior-period data available, division-by-zero handled as 0%, or the calculation is not implemented.',
  546 |           { summary: summary.map(s => ({ label: s.label, change: s.change, points: s.points })) },
  547 |         );
  548 |       }
  549 | 
  550 |       // At least one metric should have a non-zero change when there is real data
  551 |       logger.info('Asserting: BUG-REVT-05: change must not be');
  552 |       expect.soft(allZeroChange,
  553 |         '[BUG-REVT-05] Not all 3 change values should be "+0%" when real transaction data exists â€” this indicates a calculation bug',
  554 |       ).toBe(false);
  555 |       logger.pass('All assertions passed');
  556 |     });
  557 | 
  558 |     await logger.step('Step 4 â€” change format must match expected pattern N% or +N% or -N%', async () => {
  559 |       const summary = (res!.body.data as RevenueTrendData).summary;
  560 |       const PCT_PATTERN = /^[+-]?\d+(\.\d+)?%$/;
  561 | 
  562 |       for (const item of summary) {
  563 |         const chg = String(item.change ?? '');
  564 |         allure.parameter(`${item.label} change`, chg);
  565 |         logger.info('Asserting: response structure and values');
  566 |         expect(PCT_PATTERN.test(chg),
  567 |           `summary "${item.label}" change="${chg}" must match Â±N% pattern`,
  568 |         ).toBe(true);
  569 |       }
  570 |       logger.pass('All assertions passed');
  571 |     });
  572 | 
  573 |     await logger.step('Step 5 â€” "+0%" prefix is semantically wrong for a zero change', async () => {
  574 |       const summary = (res!.body.data as RevenueTrendData).summary;
  575 |       for (const item of summary) {
  576 |         if (item.change === '+0%') {
  577 |           flagIssue('TC-DREVT-006', 'BUG-REVT-05b',
  578 |             `summary "${item.label}" change="+0%" â€” a zero change is neutral and should not have a "+" prefix. ` +
  579 |             'Expected "0%" (no sign) or "0.0%" â€” the "+" is semantically misleading.',
  580 |             { label: item.label, change: item.change, expected: '"0%" or "0.0%"' },
  581 |           );
  582 |           logger.info('Asserting: response structure and values');
  583 |           expect.soft(item.change,
  584 |             `[BUG-REVT-05b] A 0% change must not have "+" prefix. Got "${item.change}", expected "0%"`,
  585 |           ).not.toBe('+0%');
  586 |         }
  587 |       }
  588 |       logger.pass('All assertions passed');
  589 |     });
  590 |   });
  591 | 
  592 |   // â”€â”€ TC-DREVT-007 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  593 |   test('TC-DREVT-007 â€” BUG-REVT-06: isPostive for "Failed" must not be false when change is "+0%"', async ({ logger }) => {
  594 |     backendLabels('TC-DREVT-007', 'BUG-REVT-06 â€” isPostive vs change Contradiction for "Failed"', 'normal');
  595 | 
  596 |     let res: supertest.Response;
  597 | 
  598 |     await logger.step('Step 1 â€” Fetch revenue-trend response', async () => {
  599 |       logger.info(`GET ${ENDPOINT}`);
  600 |       res = await get();
  601 |       logger.pass('HTTP ' + res.status + ' received');
  602 |       attachResponse('TC-DREVT-007', res, { bug: 'BUG-REVT-06' });
  603 |     });
  604 | 
  605 |     await logger.step('Step 2 â€” Validate HTTP 200', async () => {
  606 |       logger.info('Asserting: Validate HTTP 200');
> 607 |       expect(res!.status).toBe(200);
      |                           ^ Error: expect(received).toBe(expected) // Object.is equality
  608 |       logger.pass('All assertions passed');
  609 |     });
  610 | 
  611 |     await logger.step('Step 3 â€” BUG-REVT-06: each summary item: isPostive must agree with change direction', async () => {
  612 |       const summary = (res!.body.data as RevenueTrendData).summary;
  613 | 
  614 |       for (const item of summary) {
  615 |         const chg      = String(item.change ?? '');
  616 |         const isPos    = (item as unknown as Record<string, unknown>)['isPostive'];
  617 |         const chgValue = parseFloat(chg.replace(/[+%]/g, ''));
  618 | 
  619 |         allure.parameter(`${item.label} change`, chg);
  620 |         allure.parameter(`${item.label} isPostive`, String(isPos));
  621 | 
  622 |         // Case 1: change is "+0%" or "0%" â€” neutral; isPostive=false is wrong
  623 |         if (chgValue === 0 && isPos === false) {
  624 |           flagIssue('TC-DREVT-007', 'BUG-REVT-06',
  625 |             `summary "${item.label}" has isPostive=false but change="${chg}" (zero change is neutral, not negative). ` +
  626 |             'isPostive should be null or true for a 0% change.',
  627 |             { label: item.label, isPostive: isPos, change: chg },
  628 |           );
  629 |           logger.info('Asserting: BUG-REVT-06: each summary item: isPostive must ag');
  630 |           expect.soft(isPos,
  631 |             `[BUG-REVT-06] "${item.label}": isPostive=false when change="${chg}" â€” zero change is neutral, not negative`,
  632 |           ).not.toBe(false);
  633 |         }
  634 | 
  635 |         // Case 2: negative change â€” isPostive must be false
  636 |         if (chgValue < 0 && isPos !== false) {
  637 |           flagIssue('TC-DREVT-007', 'BUG-REVT-06',
  638 |             `summary "${item.label}" has isPostive=${isPos} but change="${chg}" â€” a negative change must be isPostive=false`,
  639 |             { label: item.label, isPostive: isPos, change: chg },
  640 |           );
  641 |           expect.soft(isPos,
  642 |             `[BUG-REVT-06] "${item.label}": negative change="${chg}" must have isPostive=false`,
  643 |           ).toBe(false);
  644 |         }
  645 | 
  646 |         // Case 3: positive change â€” isPostive must be true
  647 |         if (chgValue > 0 && isPos !== true) {
  648 |           flagIssue('TC-DREVT-007', 'BUG-REVT-06',
  649 |             `summary "${item.label}" has isPostive=${isPos} but change="${chg}" â€” a positive change must be isPostive=true`,
  650 |             { label: item.label, isPostive: isPos, change: chg },
  651 |           );
  652 |           expect.soft(isPos,
  653 |             `[BUG-REVT-06] "${item.label}": positive change="${chg}" must have isPostive=true`,
  654 |           ).toBe(true);
  655 |         }
  656 |       }
  657 |       logger.pass('All assertions passed');
  658 |     });
  659 |   });
  660 | 
  661 |   // â”€â”€ TC-DREVT-008 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  662 |   test('TC-DREVT-008 â€” BUG-REVT-07: all query parameters (?period=, ?startDate=, etc.) silently ignored', async ({ logger }) => {
  663 |     backendLabels('TC-DREVT-008', 'BUG-REVT-07 â€” Query Params Silently Ignored', 'normal');
  664 | 
  665 |     let resBase:    supertest.Response;
  666 |     let resWeekly:  supertest.Response;
  667 |     let resInvalid: supertest.Response;
  668 |     let resDate:    supertest.Response;
  669 | 
  670 |     await logger.step('Step 1 â€” Fetch baseline (no params)', async () => {
  671 |       logger.info(`GET ${ENDPOINT}`);
  672 |       resBase   = await get();
  673 |       logger.pass('HTTP ' + resBase.status + ' received');
  674 |       expect(resBase.status).toBe(200);
  675 |       logger.pass('All assertions passed');
  676 |     });
  677 | 
  678 |     await logger.step('Step 2 â€” Fetch with period=weekly', async () => {
  679 |       logger.info(`GET ${ENDPOINT}`);
  680 |       resWeekly = await get('period=weekly');
  681 |       logger.pass('HTTP ' + resWeekly.status + ' received');
  682 |       allure.parameter('?period=weekly status', String(resWeekly.status));
  683 |     });
  684 | 
  685 |     await logger.step('Step 3 â€” Fetch with period=invalid_value', async () => {
  686 |       logger.info(`GET ${ENDPOINT}`);
  687 |       resInvalid = await get('period=invalid_value');
  688 |       logger.pass('HTTP ' + resInvalid.status + ' received');
  689 |       allure.parameter('?period=invalid_value status', String(resInvalid.status));
  690 |     });
  691 | 
  692 |     await logger.step('Step 4 â€” Fetch with startDate/endDate', async () => {
  693 |       logger.info(`GET ${ENDPOINT}`);
  694 |       resDate = await get('startDate=2026-01-01&endDate=2026-06-01');
  695 |       logger.pass('HTTP ' + resDate.status + ' received');
  696 |       allure.parameter('?startDate/endDate status', String(resDate.status));
  697 |     });
  698 | 
  699 |     await logger.step('Step 5 â€” BUG-REVT-07a: ?period=invalid_value must return 400, not 200', async () => {
  700 |       if (resInvalid!.status === 200) {
  701 |         flagIssue('TC-DREVT-008', 'BUG-REVT-07a',
  702 |           '?period=invalid_value returns 200 OK â€” invalid parameter values must return 400 Bad Request. ' +
  703 |           'Silently accepting any value gives clients no feedback that their request was malformed.',
  704 |           { actual_status: 200, query: 'period=invalid_value', expected_status: 400 },
  705 |         );
  706 |       }
  707 |       logger.info('Asserting: BUG-REVT-07a: ?period=invalid_value must return 4');
```