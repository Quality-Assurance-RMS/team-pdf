# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-today-payments.spec.ts >> Backend (Konza) â€" Dashboard Today Payments >> TC-DTPAY-001 â€" Happy Path: 200 + data wrapper + 3 sections present
- Location: tests/API/backend-tests/backend-dashboard-today-payments.spec.ts:173:7

# Error details

```
Error: data.payments.successfullPaymentKES must be present

expect(received).toHaveProperty(path)

Expected path: "successfullPaymentKES"
Received path: []

Received value: {"failKesChange": 0, "failKesChangeDirection": "flat", "failUsdChange": 0, "failUsdChangeDirection": "flat", "failedPaymentDollar": 0, "failedPaymentKES": 0, "successKesChange": -69.4, "successKesChangeDirection": "down", "successUsdChange": 0, "successUsdChangeDirection": "flat", "successfulPaymentDollar": 0, "successfulPaymentKES": 29086}
```

# Test source

```ts
  136 | function flagIssue(tcId: string, issueId: string, description: string, actual: unknown) {
  137 |   const msg = `[${tcId}] ${issueId}: ${description} | Actual: ${JSON.stringify(actual)}`;
  138 |   console.warn(`âš  ISSUE FLAGGED â€" ${msg}`);
  139 |   allure.attachment(`âš  ${issueId}`, msg, 'text/plain');
  140 | }
  141 | 
  142 | function attachResponse(tcId: string, res: supertest.Response, extra: Record<string, unknown> = {}) {
  143 |   allure.attachment(
  144 |     `[${tcId}] Request & Response`,
  145 |     JSON.stringify({
  146 |       request:  { method: 'GET', url: `${BASE_URL}${ENDPOINT}`, headers: { Authorization: 'Bearer ***redacted***', Accept: 'application/json' } },
  147 |       response: { status: res.status, content_type: res.headers['content-type'], headers: res.headers, body: res.body, ...extra },
  148 |     }, null, 2),
  149 |     'application/json',
  150 |   );
  151 | }
  152 | 
  153 | // â"€â"€ Allure labels â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  154 | 
  155 | function backendLabels(tcId: string, title: string, severity: 'blocker' | 'critical' | 'normal' | 'minor' = 'critical') {
  156 |   allure.parentSuite('RMS Dashboard API');
  157 |   allure.suite('Backend');
  158 |   allure.subSuite('Today Payments Endpoint');
  159 |   allure.label('testId',   tcId);
  160 |   allure.label('severity', severity);
  161 |   allure.owner('Ashil Shaji');
  162 |   allure.tags('Backend', 'Konza', 'API', 'TodayPayments');
  163 |   allure.label('story', title);
  164 | }
  165 | 
  166 | // â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
  167 | // Suite
  168 | // â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•
  169 | 
  170 | test.describe('Backend (Konza) â€" Dashboard Today Payments', () => {
  171 | 
  172 |   // â"€â"€ TC-DTPAY-001 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  173 |   test('TC-DTPAY-001 â€" Happy Path: 200 + data wrapper + 3 sections present', async ({ logger }) => {
  174 |     backendLabels('TC-DTPAY-001', 'Happy Path â€" Today Payments');
  175 | 
  176 |     let res: supertest.Response;
  177 | 
  178 |     await logger.step('Step 1 â€" Build request', async () => {
  179 |       allure.parameter('Base URL',  BASE_URL);
  180 |       allure.parameter('Endpoint',  ENDPOINT);
  181 |       allure.parameter('Method',    'GET');
  182 |       allure.parameter('Auth',      `Bearer token present: ${BEARER_TOKEN ? 'YES' : 'NO'}`);
  183 |       logger.info('Asserting: request parameters are recorded');
  184 |       logger.pass('All assertions passed');
  185 |     });
  186 | 
  187 |     await logger.step('Step 2 â€" Send GET /today-payments and log response', async () => {
  188 |       logger.info('GET /api/v1/dashboard/today-payments');
  189 |       res = await get();
  190 |       logger.pass('HTTP ' + res.status + ' received');
  191 |       logResponse('TC-DTPAY-001', res);
  192 |       attachResponse('TC-DTPAY-001', res);
  193 |       allure.parameter('HTTP Status',  String(res.status));
  194 |       allure.parameter('Content-Type', String(res.headers['content-type'] ?? 'N/A'));
  195 |     });
  196 | 
  197 |     await logger.step('Step 3 â€" Validate HTTP 200', async () => {
  198 |       logger.info('Asserting: HTTP 200 and body is defined');
  199 |       expect(res!.status, 'HTTP 200 expected').toBe(200);
  200 |       expect(res!.body,   'Body must be defined').toBeDefined();
  201 |       logger.pass('All assertions passed');
  202 |     });
  203 | 
  204 |     await logger.step('Step 4 â€" Validate top-level data wrapper', async () => {
  205 |       logger.info('Asserting: response has a "data" key that is an object');
  206 |       expect(res!.body, 'Response must have a "data" key').toHaveProperty('data');
  207 |       expect(res!.body.data, '"data" must be an object').toBeInstanceOf(Object);
  208 |       logger.pass('All assertions passed');
  209 |     });
  210 | 
  211 |     await logger.step('Step 5 â€" Validate 3 required sections exist', async () => {
  212 |       const data = res!.body.data;
  213 |       logger.info('Asserting: payments, refunds, and revenue sections are present');
  214 |       expect(data, '"payments" section must be present').toHaveProperty('payments');
  215 |       expect(data, '"refunds" section must be present').toHaveProperty('refunds');
  216 |       expect(data, '"revenue" section must be present').toHaveProperty('revenue');
  217 | 
  218 |       allure.parameter('Sections present', Object.keys(data).join(', '));
  219 |       logger.pass('All assertions passed');
  220 |     });
  221 | 
  222 |     await logger.step('Step 6 â€" Validate all 6 fields in each section', async () => {
  223 |       const sections = ['payments', 'refunds', 'revenue'] as const;
  224 |       const requiredFields = [
  225 |         'successfullPaymentKES',
  226 |         'successfullPaymentDoller',
  227 |         'failerfullPaymentKES',
  228 |         'failerfullPaymentDoller',
  229 |         'isPositive',
  230 |         'postiveValue',
  231 |       ];
  232 | 
  233 |       logger.info('Asserting: all 6 required fields are present in each of the 3 sections');
  234 |       for (const section of sections) {
  235 |         for (const field of requiredFields) {
> 236 |           expect(res!.body.data[section], `data.${section}.${field} must be present`).toHaveProperty(field);
      |                                                                                       ^ Error: data.payments.successfullPaymentKES must be present
  237 |         }
  238 |         allure.parameter(`${section} fields`, requiredFields.join(', '));
  239 |       }
  240 |       logger.pass('All assertions passed');
  241 |     });
  242 | 
  243 |     await logger.step('Step 7 â€" Validate Content-Type is application/json', async () => {
  244 |       logger.info('Asserting: Content-Type header contains application/json');
  245 |       expect(res!.headers['content-type'], 'Content-Type must include application/json').toContain('application/json');
  246 |       logger.pass('All assertions passed');
  247 |     });
  248 |   });
  249 | 
  250 |   // â"€â"€ TC-DTPAY-002 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  251 |   test('TC-DTPAY-002 â€" Auth Guard: missing token  -> 401, wrong token  -> 401', async ({ logger }) => {
  252 |     backendLabels('TC-DTPAY-002', 'Auth Guard', 'blocker');
  253 | 
  254 |     let resNoAuth:   supertest.Response;
  255 |     let resWrongAuth: supertest.Response;
  256 | 
  257 |     await logger.step('Step 1 â€" Send request with NO Authorization header', async () => {
  258 |       logger.info('GET /api/v1/dashboard/today-payments (no Authorization header)');
  259 |       resNoAuth = await supertest(BASE_URL)
  260 |         .get(ENDPOINT)
  261 |         .set('Accept', 'application/json');
  262 |       logger.pass('HTTP ' + resNoAuth.status + ' received');
  263 | 
  264 |       console.log(`[TC-DTPAY-002] No-auth status: ${resNoAuth.status}`);
  265 |       console.log(`[TC-DTPAY-002] No-auth body: ${JSON.stringify(resNoAuth.body)}`);
  266 |       allure.parameter('No-auth HTTP Status', String(resNoAuth.status));
  267 |       allure.parameter('No-auth Error',       String(resNoAuth.body?.message ?? resNoAuth.body?.error ?? 'N/A'));
  268 |     });
  269 | 
  270 |     await logger.step('Step 2 â€" Send request with INVALID token', async () => {
  271 |       logger.info('GET /api/v1/dashboard/today-payments (invalid Bearer token)');
  272 |       resWrongAuth = await supertest(BASE_URL)
  273 |         .get(ENDPOINT)
  274 |         .set('Authorization', 'Bearer invalidtoken.abc.xyz')
  275 |         .set('Accept', 'application/json');
  276 |       logger.pass('HTTP ' + resWrongAuth.status + ' received');
  277 | 
  278 |       console.log(`[TC-DTPAY-002] Wrong-auth status: ${resWrongAuth.status}`);
  279 |       console.log(`[TC-DTPAY-002] Wrong-auth body: ${JSON.stringify(resWrongAuth.body)}`);
  280 |       allure.parameter('Wrong-auth HTTP Status', String(resWrongAuth.status));
  281 |       allure.parameter('Wrong-auth Error',       String(resWrongAuth.body?.message ?? resWrongAuth.body?.error ?? 'N/A'));
  282 |     });
  283 | 
  284 |     await logger.step('Step 3 â€" Validate both return 401', async () => {
  285 |       logger.info('Asserting: both no-auth and wrong-auth requests return 401');
  286 |       expect(resNoAuth!.status,   'Missing token must return 401').toBe(401);
  287 |       expect(resWrongAuth!.status,'Invalid token must return 401').toBe(401);
  288 |       logger.pass('All assertions passed');
  289 |     });
  290 | 
  291 |     await logger.step('Step 4 â€" Validate 401 error body structure', async () => {
  292 |       logger.info('Asserting: 401 error bodies have correct status and message fields');
  293 |       expect(resNoAuth!.body,    'No-auth body must be defined').toBeDefined();
  294 |       expect(resWrongAuth!.body, 'Wrong-auth body must be defined').toBeDefined();
  295 | 
  296 |       expect(resNoAuth!.body.status,  'No-auth body.status must be 401').toBe(401);
  297 |       expect(resWrongAuth!.body.status,'Wrong-auth body.status must be 401').toBe(401);
  298 | 
  299 |       expect(resNoAuth!.body.message,   'No-auth must return "Missing Authorization token"').toBe('Missing Authorization token');
  300 |       expect(resWrongAuth!.body.message,'Wrong-auth must return "Invalid or expired token"').toBe('Invalid or expired token');
  301 |       logger.pass('All assertions passed');
  302 |     });
  303 | 
  304 |     attachResponse('TC-DTPAY-002', resNoAuth!,    { scenario: 'no-auth' });
  305 |     attachResponse('TC-DTPAY-002', resWrongAuth!, { scenario: 'wrong-auth' });
  306 |   });
  307 | 
  308 |   // â"€â"€ TC-DTPAY-003 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  309 |   test('TC-DTPAY-003 â€" BUG-TPAY-01: Field name typos in API response', async ({ logger }) => {
  310 |     backendLabels('TC-DTPAY-003', 'BUG-TPAY-01 â€" Field Name Typos', 'critical');
  311 | 
  312 |     let res: supertest.Response;
  313 | 
  314 |     await logger.step('Step 1 â€" Fetch today-payments response', async () => {
  315 |       logger.info('GET /api/v1/dashboard/today-payments');
  316 |       res = await get();
  317 |       logger.pass('HTTP ' + res.status + ' received');
  318 |       logResponse('TC-DTPAY-003', res, '(typo check)');
  319 |       attachResponse('TC-DTPAY-003', res, { bug: 'BUG-TPAY-01' });
  320 |     });
  321 | 
  322 |     await logger.step('Step 2 â€" Validate HTTP 200', async () => {
  323 |       logger.info('Asserting: HTTP 200');
  324 |       expect(res!.status).toBe(200);
  325 |       logger.pass('All assertions passed');
  326 |     });
  327 | 
  328 |     await logger.step('Step 3 â€" Check "successfullPaymentKES" typo (double-l)', async () => {
  329 |       // Correct spelling: "successfulPaymentKES" (single l)
  330 |       // Actual API response: "successfullPaymentKES" (double l â€" typo)
  331 |       const hasTypo    = 'successfullPaymentKES' in res!.body.data.payments;
  332 |       const hasCorrect = 'successfulPaymentKES'  in res!.body.data.payments;
  333 | 
  334 |       if (hasTypo) {
  335 |         flagIssue('TC-DTPAY-003', 'BUG-TPAY-01a',
  336 |           'Field "successfullPaymentKES" has a double-l typo â€" correct name is "successfulPaymentKES"',
```