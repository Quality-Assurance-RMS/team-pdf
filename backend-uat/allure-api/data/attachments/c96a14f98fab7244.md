# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-monthly-revenue.spec.ts >> Backend (Konza) â€” Dashboard Monthly Revenue >> TC-DMREV-007 â€” BUG-MREV-04: CRITICAL â€” /revenue-trend KES total is inflated by USD amounts (currency mixing)
- Location: tests/API/backend-tests/backend-dashboard-monthly-revenue.spec.ts:509:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 503
```

# Test source

```ts
  417 | 
  418 |     await logger.step('Step 3 â€” Verify year=-1 correctly returns 400 (contrast)', async () => {
  419 |       logger.info(`GET ${ENDPOINT}`);
  420 |       const resNeg = await get('year=-1');
  421 |       logger.pass('HTTP ' + resNeg.status + ' received');
  422 |       logger.info('Asserting: response structure and values');
  423 |       expect(resNeg.status, 'year=-1 must return 400 (confirmed working)').toBe(400);
  424 |       expect(resNeg.body.message, 'year=-1 error must mention 4-digit year').toContain('4-digit');
  425 |       allure.parameter('year=-1 status',  String(resNeg.status));
  426 |       allure.parameter('year=-1 message', String(resNeg.body?.message));
  427 |       logger.pass('All assertions passed');
  428 |     });
  429 |   });
  430 | 
  431 |   // â”€â”€ TC-DMREV-005 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  432 |   test('TC-DMREV-005 â€” BUG-MREV-02: year= (empty string) silently defaults to current year', async ({ logger }) => {
  433 |     backendLabels('TC-DMREV-005', 'BUG-MREV-02 â€” Empty year Param Not Rejected', 'normal');
  434 |     let res: supertest.Response;
  435 | 
  436 |     await logger.step('Step 1 â€” Send GET with year= (empty string)', async () => {
  437 |       logger.info(`GET ${ENDPOINT}`);
  438 |       res = await get('year=');
  439 |       logger.pass('HTTP ' + res.status + ' received');
  440 |       console.log(`[TC-DMREV-005] year= â†’ ${res.status}  data.year=${res.body.data?.year}`);
  441 |       allure.parameter('year= status',    String(res.status));
  442 |       allure.parameter('year= data.year', String(res.body.data?.year ?? 'N/A'));
  443 |       attachResponse('TC-DMREV-005', res, { bug: 'BUG-MREV-02', query: 'year=' });
  444 |     });
  445 | 
  446 |     await logger.step('Step 2 â€” BUG-MREV-02: empty year= must return 400', async () => {
  447 |       logger.info('Asserting: BUG-MREV-02 — empty year= must return 400');
  448 |       if (res!.status === 200) {
  449 |         flagIssue('TC-DMREV-005', 'BUG-MREV-02',
  450 |           'GET /monthly-revenue?year= (empty string) returns 200 and silently defaults to current year. ' +
  451 |           'An empty string is not a valid year value â€” the server must return 400 Bad Request ' +
  452 |           'or clearly document that an empty year means “use current year”.',
  453 |           {
  454 |             year_param:    '',
  455 |             actual_status: res!.status,
  456 |             actual_year:   res!.body.data?.year,
  457 |             expected_status: 400,
  458 |             note: 'Silent fallback to current year provides no feedback to the caller that their param was malformed',
  459 |           },
  460 |         );
  461 |       }
  462 |       expect.soft(res!.status,
  463 |         '[BUG-MREV-02] year= (empty) must return 400 or be documented as "use current year default"',
  464 |       ).toBe(400);
  465 |       logger.pass('All assertions passed');
  466 |     });
  467 |   });
  468 | 
  469 |   // â”€â”€ TC-DMREV-006 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  470 |   test('TC-DMREV-006 â€” BUG-MREV-03: year=1000 is accepted â€” no sensible lower-bound validation', async ({ logger }) => {
  471 |     backendLabels('TC-DMREV-006', 'BUG-MREV-03 â€” Unreasonable Historical Year Accepted', 'minor');
  472 |     let res: supertest.Response;
  473 | 
  474 |     await logger.step('Step 1 â€” Send GET with year=1000', async () => {
  475 |       logger.info(`GET ${ENDPOINT}`);
  476 |       res = await get('year=1000');
  477 |       logger.pass('HTTP ' + res.status + ' received');
  478 |       console.log(`[TC-DMREV-006] year=1000 â†’ ${res.status}  data.year=${res.body.data?.year}`);
  479 |       allure.parameter('year=1000 status',    String(res.status));
  480 |       allure.parameter('year=1000 data.year', String(res.body.data?.year ?? 'N/A'));
  481 |       attachResponse('TC-DMREV-006', res, { bug: 'BUG-MREV-03', query: 'year=1000' });
  482 |     });
  483 | 
  484 |     await logger.step('Step 2 â€” BUG-MREV-03: year=1000 must return 400', async () => {
  485 |       logger.info('Asserting: BUG-MREV-03 — year=1000 must return 400');
  486 |       if (res!.status === 200) {
  487 |         flagIssue('TC-DMREV-006', 'BUG-MREV-03',
  488 |           'GET /monthly-revenue?year=1000 returns 200 OK with an empty dataset. ' +
  489 |           'Year 1000 predates electronic financial systems by approximately 1 024 years. ' +
  490 |           'A sensible minimum year (e.g. 2000 or system inception year) should be enforced. ' +
  491 |           'Accepting any 4-digit year wastes a database query returning meaningless data.',
  492 |           {
  493 |             year_param:    1000,
  494 |             actual_status: res!.status,
  495 |             actual_data:   { year: res!.body.data?.year, all_zeros: true },
  496 |             expected_status: 400,
  497 |             suggested_validation: 'year >= 2020 (or system start year)',
  498 |           },
  499 |         );
  500 |       }
  501 |       expect.soft(res!.status,
  502 |         '[BUG-MREV-03] year=1000 must return 400 â€” a sensible lower bound (e.g. â‰¥ 2000) should be enforced',
  503 |       ).toBe(400);
  504 |       logger.pass('All assertions passed');
  505 |     });
  506 |   });
  507 | 
  508 |   // â”€â”€ TC-DMREV-007 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  509 |   test('TC-DMREV-007 â€” BUG-MREV-04: CRITICAL â€” /revenue-trend KES total is inflated by USD amounts (currency mixing)', async ({ logger }) => {
  510 |     backendLabels('TC-DMREV-007', 'BUG-MREV-04 â€” Cross-Endpoint Currency Mixing Bug', 'blocker');
  511 |     let res: supertest.Response;
  512 | 
  513 |     await logger.step('Step 1 â€” Fetch monthly-revenue for current year', async () => {
  514 |       logger.info(`GET ${ENDPOINT}`);
  515 |       res = await get();
  516 |       logger.pass('HTTP ' + res.status + ' received');
> 517 |       expect(res.status).toBe(200);
      |                          ^ Error: expect(received).toBe(expected) // Object.is equality
  518 |       logResponse('TC-DMREV-007', res, '(cross-endpoint check)');
  519 |       attachResponse('TC-DMREV-007', res, { bug: 'BUG-MREV-04' });
  520 |       logger.pass('All assertions passed');
  521 |     });
  522 | 
  523 |     await logger.step('Step 2 â€” BUG-MREV-04: Confirm cross-endpoint data inconsistency', async () => {
  524 |       const s = res!.body.data.summary as Summary;
  525 | 
  526 |       const monthlyGrossKes = s.total_gross_kes;
  527 |       const monthlyGrossUsd = s.total_gross_usd;
  528 |       const revTrendKes     = REVENUE_TREND_TOTAL_KES; // 18,633,694
  529 | 
  530 |       const sumKesAndUsd = monthlyGrossKes + monthlyGrossUsd;
  531 |       const diffFromTrend = Math.abs(revTrendKes - monthlyGrossKes);
  532 |       const diffFromUsd   = Math.abs(diffFromTrend - monthlyGrossUsd);
  533 |       const isCurrencyMix = diffFromUsd < 1; // within 1 unit (floating point tolerance)
  534 | 
  535 |       allure.parameter('/monthly-revenue total_gross_kes', monthlyGrossKes.toLocaleString());
  536 |       allure.parameter('/monthly-revenue total_gross_usd', monthlyGrossUsd.toLocaleString());
  537 |       allure.parameter('/revenue-trend KES total (reported)', revTrendKes.toLocaleString());
  538 |       allure.parameter('KES + USD (if mixed 1:1)', sumKesAndUsd.toFixed(2));
  539 |       allure.parameter('Trend minus Monthly KES diff', diffFromTrend.toFixed(2));
  540 |       allure.parameter('Diff â‰ˆ gross_usd amount?', String(isCurrencyMix));
  541 |       allure.parameter('CURRENCY MIXING CONFIRMED', String(isCurrencyMix));
  542 | 
  543 |       if (isCurrencyMix) {
  544 |         flagIssue('TC-DMREV-007', 'BUG-MREV-04',
  545 |           'CRITICAL CROSS-ENDPOINT BUG: The /revenue-trend endpoint reports its KES total as ' +
  546 |           `${revTrendKes.toLocaleString()} KES, while /monthly-revenue reports total_gross_kes=${monthlyGrossKes.toLocaleString()} KES. ` +
  547 |           `The difference is ${diffFromTrend.toFixed(2)}, which equals total_gross_usd (${monthlyGrossUsd.toLocaleString()} USD) to within 1 unit. ` +
  548 |           'This proves /revenue-trend computes its KES total by adding KES + USD amounts treating 1 USD = 1 KES. ' +
  549 |           `The correct KES total is ${monthlyGrossKes.toLocaleString()} KES. ` +
  550 |           'Reported figure is inflated by the full USD amount (222,987 KES worth of USD is counted twice at wrong rate).',
  551 |           {
  552 |             revenue_trend_reported_kes: revTrendKes,
  553 |             monthly_revenue_gross_kes:  monthlyGrossKes,
  554 |             monthly_revenue_gross_usd:  monthlyGrossUsd,
  555 |             kes_plus_usd_at_1to1:       sumKesAndUsd,
  556 |             difference_from_trend:      diffFromTrend,
  557 |             currency_mixing_confirmed:  isCurrencyMix,
  558 |             impact: 'Revenue figures shown in /revenue-trend chart are overstated by ~KES 222,987 (the USD total)',
  559 |             root_cause: 'Likely: SUM(gross_amount) across all currencies without currency conversion â€” USD amounts added to KES as-is',
  560 |           },
  561 |         );
  562 |       }
  563 | 
  564 |       // The two endpoints should agree on the KES total when data is current
  565 |       // We use expect.soft because the fix is in /revenue-trend, not here
  566 |       logger.info('Asserting: BUG-MREV-04: Confirm cross-endpoint data inconsis');
  567 |       expect.soft(isCurrencyMix,
  568 |         `[BUG-MREV-04] /revenue-trend KES total (${revTrendKes.toLocaleString()}) = monthly_gross_kes(${monthlyGrossKes.toLocaleString()}) + monthly_gross_usd(${monthlyGrossUsd.toLocaleString()}) â€” USD treated as KES`,
  569 |       ).toBe(false);
  570 | 
  571 |       // Both totals must be positive when there is data
  572 |       expect(monthlyGrossKes, 'total_gross_kes must be > 0 when year has transactions').toBeGreaterThan(0);
  573 |       expect(monthlyGrossUsd, 'total_gross_usd must be > 0 when year has transactions').toBeGreaterThan(0);
  574 |       logger.pass('All assertions passed');
  575 |     });
  576 |   });
  577 | 
  578 |   // â”€â”€ TC-DMREV-008 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  579 |   test('TC-DMREV-008 â€” BUG-MREV-05: conversion_ratio_pct must be null when no data, not 0.0', async ({ logger }) => {
  580 |     backendLabels('TC-DMREV-008', 'BUG-MREV-05 â€” conversion_ratio_pct=0.0 for Zero-Data Years', 'minor');
  581 | 
  582 |     await logger.step('Step 1 â€” Fetch year=2025 (known zero-data year)', async () => {
  583 |       logger.info(`GET ${ENDPOINT}`);
  584 |       const res = await get('year=2025');
  585 |       logger.pass('HTTP ' + res.status + ' received');
  586 |       expect(res.status).toBe(200);
  587 | 
  588 |       const s = res.body.data.summary as Summary;
  589 |       allure.parameter('year=2025 total_payments', String(s.total_payments));
  590 |       allure.parameter('year=2025 conversion_ratio_pct', String(s.conversion_ratio_pct));
  591 |       attachResponse('TC-DMREV-008', res, { bug: 'BUG-MREV-05', query: 'year=2025' });
  592 | 
  593 |       if (s.total_payments === 0 && s.conversion_ratio_pct === 0) {
  594 |         flagIssue('TC-DMREV-008', 'BUG-MREV-05',
  595 |           'conversion_ratio_pct=0.0 when total_payments=0 â€” this is division by zero (0 successful / 0 total). ' +
  596 |           'A ratio of 0.0% is misleading because it implies 0% of transactions succeeded, not that there were no transactions. ' +
  597 |           'When there is no data, conversion_ratio_pct should be null (or omitted) to signal "not applicable".',
  598 |           {
  599 |             year: 2025,
  600 |             total_payments: s.total_payments,
  601 |             conversion_ratio_pct: s.conversion_ratio_pct,
  602 |             expected: null,
  603 |             issue: 'Division by zero returns 0.0 instead of null â€” "0% conversion" misrepresents "no data"',
  604 |           },
  605 |         );
  606 |       }
  607 | 
  608 |       expect.soft(s.conversion_ratio_pct,
  609 |         '[BUG-MREV-05] conversion_ratio_pct must be null (not 0.0) when total_payments=0 â€” there were no transactions, not 0% successful ones',
  610 |       ).toBeNull();
  611 |       logger.pass('All assertions passed');
  612 |     });
  613 | 
  614 |     await logger.step('Step 2 â€” Verify current year has a valid conversion ratio > 0', async () => {
  615 |       logger.info(`GET ${ENDPOINT}`);
  616 |       const res = await get();
  617 |       logger.pass('HTTP ' + res.status + ' received');
```