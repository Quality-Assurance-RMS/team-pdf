# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: API/backend-tests/backend-dashboard-status-currency.spec.ts >> Backend (Konza) â€" Dashboard Status & Currency >> TC-DSH-007 â€" Period filtering: query params must affect returned data (DASH-ISSUE-1)
- Location: tests/API/backend-tests/backend-dashboard-status-currency.spec.ts:739:7

# Error details

```
Error: period=week  -> 200

expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 400
```

# Test source

```ts
  681 |       logger.pass('All assertions passed');
  682 |     });
  683 | 
  684 |     await logger.step('Step 6 â€" Validate numeric metric fields and flag string-type issues', async () => {
  685 |       const d  = res!.body?.data ?? res!.body;
  686 |       const pm = d.peakMetrics as Record<string, unknown>;
  687 | 
  688 |       logger.info('Asserting: numeric metric fields are numbers and change-percentage fields do not embed units in strings');
  689 |       // â"€â"€ DASH-ISSUE-2: peakTransactionsPerDay is a string â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  690 |       if (typeof pm.peakTransactionsPerDay === 'string') {
  691 |         flagIssue('TC-DSH-006', 'DASH-ISSUE-2',
  692 |           'peakMetrics.peakTransactionsPerDay is a string â€" should be a number',
  693 |           pm.peakTransactionsPerDay,
  694 |         );
  695 |       }
  696 |       expect.soft(typeof pm.peakTransactionsPerDay,
  697 |         '[DASH-ISSUE-2] peakMetrics.peakTransactionsPerDay should be a number, not a string',
  698 |       ).toBe('number');
  699 | 
  700 |       // â"€â"€ DASH-ISSUE-3: avgTPM and maxTPM embed the unit in the string value â"€â"€â"€â"€
  701 |       for (const field of ['avgTPM', 'maxTPM'] as const) {
  702 |         const val = String(pm[field] ?? '');
  703 |         if (val.includes('TPM')) {
  704 |           flagIssue('TC-DSH-006', 'DASH-ISSUE-3',
  705 |             `peakMetrics.${field} embeds the unit "TPM" inside the string value â€" should be a plain number with a separate unit field`,
  706 |             pm[field],
  707 |           );
  708 |         }
  709 |         expect.soft(typeof pm[field],
  710 |           `[DASH-ISSUE-3] peakMetrics.${field} should be a number, not a string with embedded unit`,
  711 |         ).toBe('number');
  712 | 
  713 |         allure.parameter(`peakMetrics.${field}`, val + (val.includes('TPM') ? ' âš  DASH-ISSUE-3' : ''));
  714 |       }
  715 | 
  716 |       // â"€â"€ DASH-ISSUE-4: change percentages are formatted strings â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  717 |       for (const field of ['peakTxnDayChange', 'avgTpmChange', 'maxTpmChange'] as const) {
  718 |         const val = String(pm[field] ?? '');
  719 |         if (val.includes('%') || val.startsWith('+') || val.startsWith('-')) {
  720 |           flagIssue('TC-DSH-006', 'DASH-ISSUE-4',
  721 |             `peakMetrics.${field} is a formatted string "${val}" â€" should be a numeric value like 161.3 (consistent with other APIs returning numeric change_pct)`,
  722 |             pm[field],
  723 |           );
  724 |         }
  725 |         expect.soft(typeof pm[field],
  726 |           `[DASH-ISSUE-4] peakMetrics.${field} should be a number, not a formatted string`,
  727 |         ).toBe('number');
  728 | 
  729 |         allure.parameter(`peakMetrics.${field}`, val + (typeof pm[field] === 'string' ? ' âš  DASH-ISSUE-4' : ''));
  730 |       }
  731 | 
  732 |       allure.parameter('peakMetrics.peakTransactionsPerDay', String(pm.peakTransactionsPerDay) + (typeof pm.peakTransactionsPerDay === 'string' ? ' âš  DASH-ISSUE-2' : ''));
  733 |       allure.parameter('peakMetrics.peakHourWindow',         String(pm.peakHourWindow));
  734 |       logger.pass('All assertions passed');
  735 |     });
  736 |   });
  737 | 
  738 |   // â"€â"€ TC-DSH-007 â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  739 |   test('TC-DSH-007 â€" Period filtering: query params must affect returned data (DASH-ISSUE-1)', async ({ logger }) => {
  740 |     backendLabels('TC-DSH-007', 'Status Currency Endpoint', 'Period Filtering â€" date params respected', 'blocker');
  741 | 
  742 |     let resNoParams:     supertest.Response;
  743 |     let resPeriodWeek:   supertest.Response;
  744 |     let resPeriodToday:  supertest.Response;
  745 |     let resCustomRange:  supertest.Response;
  746 |     let resInvalidParam: supertest.Response;
  747 | 
  748 |     await logger.step('Step 1 â€" Build requests for all period variants', async () => {
  749 |       allure.parameter('Base URL',   BASE_URL);
  750 |       allure.parameter('Endpoint',   ENDPOINT);
  751 |       allure.parameter('Method',     'GET');
  752 |       allure.parameter('Scenarios',  'no-params, period=week, period=today, period=custom, period=invalid_xyz');
  753 |       logger.info('Asserting: request parameters are recorded for all period variants');
  754 |       logger.pass('All assertions passed');
  755 |     });
  756 | 
  757 |     await logger.step('Step 2 â€" Send all five requests', async () => {
  758 |       logger.info('GET /api/v1/dashboard/status-currency (5 variants: no-params, period=week, period=today, period=custom, period=invalid_xyz)');
  759 |       resNoParams     = await get();
  760 |       resPeriodWeek   = await get({ period: 'week' });
  761 |       resPeriodToday  = await get({ period: 'today' });
  762 |       resCustomRange  = await get({ period: 'custom', from_date: '2026-01-01', to_date: '2026-05-31' });
  763 |       resInvalidParam = await get({ period: 'invalid_xyz' });
  764 |       logger.pass('HTTP ' + resNoParams.status + ' received (no-params)');
  765 | 
  766 |       for (const [label, r] of [
  767 |         ['no-params', resNoParams], ['period=week', resPeriodWeek],
  768 |         ['period=today', resPeriodToday], ['period=custom', resCustomRange],
  769 |         ['period=invalid_xyz', resInvalidParam],
  770 |       ] as [string, supertest.Response][]) {
  771 |         console.log(`\n[TC-DSH-007] ${label}  -> status=${r.status}`);
  772 |         allure.attachment(`[TC-DSH-007] ${label}`,
  773 |           JSON.stringify({ status: r.status, body: r.body }, null, 2), 'application/json',
  774 |         );
  775 |       }
  776 |     });
  777 | 
  778 |     await logger.step('Step 3 â€" All valid period values must return HTTP 200', async () => {
  779 |       logger.info('Asserting: no-params, period=week, period=today, and period=custom all return 200');
  780 |       expect(resNoParams!.status,    'no-params  -> 200').toBe(200);
> 781 |       expect(resPeriodWeek!.status,  'period=week  -> 200').toBe(200);
      |                                                             ^ Error: period=week  -> 200
  782 |       expect(resPeriodToday!.status, 'period=today  -> 200').toBe(200);
  783 |       expect(resCustomRange!.status, 'period=custom  -> 200').toBe(200);
  784 |       logger.pass('All assertions passed');
  785 |     });
  786 | 
  787 |     await logger.step('Step 4 â€" Invalid period value must return 400 (flag DASH-ISSUE-1 if 200)', async () => {
  788 |       // An invalid period parameter should be rejected with 400.
  789 |       // If it returns 200 with identical data it means the param is silently ignored.
  790 |       if (resInvalidParam!.status === 200) {
  791 |         flagIssue('TC-DSH-007', 'DASH-ISSUE-1',
  792 |           'period=invalid_xyz returned HTTP 200 instead of 400 â€" invalid parameter values are silently accepted',
  793 |           { status: resInvalidParam!.status, body: resInvalidParam!.body },
  794 |         );
  795 |       }
  796 | 
  797 |       logger.info('Asserting: period=invalid_xyz returns 400, not 200 (invalid params should be rejected)');
  798 |       expect.soft(resInvalidParam!.status,
  799 |         '[DASH-ISSUE-1] period=invalid_xyz should return 400, not 200',
  800 |       ).toBe(400);
  801 | 
  802 |       allure.parameter('period=invalid_xyz status', String(resInvalidParam!.status) + (resInvalidParam!.status === 200 ? ' âš  DASH-ISSUE-1' : ''));
  803 |       logger.pass('All assertions passed');
  804 |     });
  805 | 
  806 |     await logger.step('Step 5 â€" period=today and period=week must return DIFFERENT data (flag DASH-ISSUE-1 if identical)', async () => {
  807 |       const bodyToday = JSON.stringify(resPeriodToday!.body);
  808 |       const bodyWeek  = JSON.stringify(resPeriodWeek!.body);
  809 |       const bodyNone  = JSON.stringify(resNoParams!.body);
  810 | 
  811 |       const todayEqualsWeek = bodyToday === bodyWeek;
  812 |       const todayEqualsNone = bodyToday === bodyNone;
  813 |       const weekEqualsNone  = bodyWeek  === bodyNone;
  814 | 
  815 |       if (todayEqualsWeek || todayEqualsNone || weekEqualsNone) {
  816 |         flagIssue('TC-DSH-007', 'DASH-ISSUE-1',
  817 |           'period query parameter is completely ignored â€" period=today, period=week, and no-params all return identical response bodies. Date filtering is not implemented.',
  818 |           {
  819 |             'today === week':       todayEqualsWeek,
  820 |             'today === no-params':  todayEqualsNone,
  821 |             'week === no-params':   weekEqualsNone,
  822 |           },
  823 |         );
  824 |       }
  825 | 
  826 |       logger.info('Asserting: period=today and period=week return different data from each other and from no-params');
  827 |       expect.soft(todayEqualsWeek,
  828 |         '[DASH-ISSUE-1] period=today and period=week should return DIFFERENT data â€" they are currently identical',
  829 |       ).toBe(false);
  830 | 
  831 |       expect.soft(todayEqualsNone,
  832 |         '[DASH-ISSUE-1] period=today and no-params should return DIFFERENT data â€" they are currently identical',
  833 |       ).toBe(false);
  834 | 
  835 |       allure.parameter('today === week response',       String(todayEqualsWeek)  + (todayEqualsWeek  ? ' âš  DASH-ISSUE-1 IDENTICAL' : ' â€" DIFFERENT (OK)'));
  836 |       allure.parameter('today === no-params response',  String(todayEqualsNone)  + (todayEqualsNone  ? ' âš  DASH-ISSUE-1 IDENTICAL' : ' â€" DIFFERENT (OK)'));
  837 |       allure.parameter('week === no-params response',   String(weekEqualsNone)   + (weekEqualsNone   ? ' âš  DASH-ISSUE-1 IDENTICAL' : ' â€" DIFFERENT (OK)'));
  838 |       logger.pass('All assertions passed');
  839 |     });
  840 | 
  841 |     await logger.step('Step 6 â€" period=custom date range must return different data from no-params', async () => {
  842 |       const bodyCustom = JSON.stringify(resCustomRange!.body);
  843 |       const bodyNone   = JSON.stringify(resNoParams!.body);
  844 |       const identical  = bodyCustom === bodyNone;
  845 | 
  846 |       if (identical) {
  847 |         flagIssue('TC-DSH-007', 'DASH-ISSUE-1',
  848 |           'period=custom&from_date=2026-01-01&to_date=2026-05-31 returns identical data to no-params â€" date range filtering is not applied',
  849 |           { identical },
  850 |         );
  851 |       }
  852 | 
  853 |       logger.info('Asserting: period=custom with specific date range returns different data from no-params');
  854 |       expect.soft(identical,
  855 |         '[DASH-ISSUE-1] period=custom with specific date range should return DIFFERENT data from no-params',
  856 |       ).toBe(false);
  857 | 
  858 |       allure.parameter('custom === no-params response', String(identical) + (identical ? ' âš  DASH-ISSUE-1 IDENTICAL' : ' â€" DIFFERENT (OK)'));
  859 |       logger.pass('All assertions passed');
  860 |     });
  861 |   });
  862 | 
  863 | });
  864 | 
```