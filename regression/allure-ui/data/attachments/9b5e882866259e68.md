# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/Verification/verify-otp-tc1-full-login-otp-dashboard.spec.ts >> Verification — OTP Flow >> TC1 — Full login with a correct OTP reaches the dashboard
- Location: tests/UI/Verification/verify-otp-tc1-full-login-otp-dashboard.spec.ts:18:7

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/auth\/verification/
Received string: "https://api.rms.dev.demo-fsit.com/auth/verification"
Timeout: 30000ms

Call log:
  - Expect "not toHaveURL" with timeout 30000ms
    61 × unexpected value "https://api.rms.dev.demo-fsit.com/auth/verification"

```

```yaml
- img "Unified Control"
- paragraph: Government services, now at your fingertips
- img
- heading "RMS" [level=1]
- text: National Treasury
- heading "Verification" [level=1]
- paragraph: Please enter the verification code sent to your registered Email ID
- paragraph: ashil.shaji@fortunesoftit.com
- textbox
- textbox
- textbox
- textbox
- text: "OTP expires in: 02:24 Didn't Receive OTP?"
- button "Resend" [disabled]
- link "Back to Login":
  - /url: /auth/login
- region "Notifications alt+T"
```

# Test source

```ts
  1  | import { test, expect } from '../fixtures';
  2  | import { allure } from 'allure-playwright';
  3  | import { LoginPage } from '../../pages/LoginPage';
  4  | import { VerificationPage } from '../../pages/VerificationPage';
  5  | import { fetchOtpFromEmail } from '../../utils/graphMailHelper';
  6  | 
  7  | const EMAIL = process.env.LOGIN_EMAIL!;
  8  | const PASSWORD = process.env.LOGIN_PASSWORD!;
  9  | 
  10 | test.describe('Verification — OTP Flow', () => {
  11 |   // These tests exercise the raw OTP verification flow starting from the login
  12 |   // page, so the global storageState must be cleared to prevent an automatic
  13 |   // redirect to /dashboard before the login form is even shown.
  14 |   test.use({ storageState: { cookies: [], origins: [] } });
  15 | 
  16 |   test.setTimeout(120000); // Allow time for email delivery (up to 90s polling)
  17 | 
  18 |   test('TC1 — Full login with a correct OTP reaches the dashboard', async ({ page, logger }) => {
  19 |     await allure.description('Verifies that logging in, receiving an OTP by email, and entering it correctly redirects the user to the dashboard.');
  20 |     await allure.label('feature',  'Verification');
  21 |     await allure.label('story',    'OTP Flow');
  22 |     await allure.label('severity', 'blocker');
  23 |     await allure.label('priority', 'P0');
  24 |     await allure.label('testId',   'VERIFY-OTP-TC1');
  25 | 
  26 |     await logger.step('Step 1 — Verifies that logging in, receiving an OTP by email, and entering it correctly redirects the user to the dashboard', async () => {
  27 |       logger.info('Test scope: full OTP login flow reaching the dashboard');
  28 |     });
  29 | 
  30 |     // Record time just before login so we only fetch the fresh OTP
  31 |     const loginTime = new Date();
  32 | 
  33 |     await logger.step('Step 2 — Navigate to login page and submit credentials', async () => {
  34 |       logger.info('Opening the login page');
  35 |       const loginPage = new LoginPage(page);
  36 |       await loginPage.goto();
  37 |       logger.info('Submitting login credentials');
  38 |       await loginPage.login(EMAIL, PASSWORD);
  39 |       logger.pass('Credentials submitted — expecting redirect to OTP verification');
  40 |     });
  41 | 
  42 |     const verificationPage = new VerificationPage(page);
  43 | 
  44 |     await logger.step('Step 3 — Verify OTP page is loaded', async () => {
  45 |       logger.info('Waiting for OTP verification page to render');
  46 |       await verificationPage.assertPageLoaded();
  47 |       logger.pass('OTP verification page is loaded');
  48 |       logger.info('Checking Resend and Back to Login links are visible');
  49 |       await expect(verificationPage.resendLink).toBeVisible();
  50 |       logger.pass('Resend OTP link is visible');
  51 |       await expect(verificationPage.backToLoginLink).toBeVisible();
  52 |       logger.pass('Back to Login link is visible');
  53 |     });
  54 | 
  55 |     let otp = '';
  56 |     await logger.step('Step 4 — Fetch OTP from email', async () => {
  57 |       logger.info('Polling mailbox for fresh OTP sent after login time');
  58 |       // Fetch only the OTP sent after loginTime (ignores stale previous OTPs)
  59 |       otp = await fetchOtpFromEmail(EMAIL, process.env.OTP_SENDER_EMAIL!, loginTime);
  60 |       expect(otp).toMatch(/^\d{4}$/);
  61 |       logger.pass('OTP received from email: ' + otp);
  62 |     });
  63 | 
  64 |     await logger.step('Step 5 — Enter OTP and verify redirect to dashboard', async () => {
  65 |       logger.info('Entering the 4-digit OTP in the verification form');
  66 |       await verificationPage.enterOtp(otp);
  67 |       logger.pass('OTP entered successfully');
  68 |       logger.info('Waiting for redirect away from verification page');
  69 |       // Widened from 15s: this hits the same dev/demo backend that shows
  70 |       // measurable slowdowns under concurrent load (see load-test findings),
  71 |       // and the full suite runs 20 parallel workers against it simultaneously.
> 72 |       await expect(page).not.toHaveURL(/\/auth\/verification/, { timeout: 30000 });
     |                              ^ Error: expect(page).not.toHaveURL(expected) failed
  73 |       logger.pass('Successfully navigated away from verification — OTP accepted: ' + page.url());
  74 |     });
  75 | 
  76 |     await logger.step('Step 6 — Hold on dashboard for observation', async () => {
  77 |       logger.info('Waiting 10 seconds to observe the final dashboard state');
  78 |       await page.waitForTimeout(10000);
  79 |       logger.pass('Observation period complete — test finished successfully');
  80 |     });
  81 |   });
  82 | });
  83 | 
```