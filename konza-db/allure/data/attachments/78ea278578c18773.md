# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.konza.ts >> authenticate via TaifaSSO and save Konza session
- Location: tests/auth.setup.konza.ts:15:6

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /^sign in$/i })
Expected: visible
Timeout: 30000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 30000ms
  - waiting for getByRole('heading', { name: /^sign in$/i })
    - waiting for" http://sso.uat.konza/identity/o/authorize?client_id=b27e82fd06434093a906c1cc08ffbc53&response_type=code&scope=openid+nationality+identification+profile+phone+email&redirect_uri=https%3A%2F%2Frms.uat.…" navigation to finish...
    - navigated to "http://sso.uat.konza/signin/identify/"

```

```yaml
- navigation:
  - link "TaifaSSO — Public Service Single Sign On":
    - /url: /
    - img "TaifaSSO — Public Service Single Sign On"
  - link "Home":
    - /url: /
  - link "About":
    - /url: /#about
  - link "Capabilities":
    - /url: /#capabilities
  - link "Compliance":
    - /url: /#compliance
  - link "FAQ":
    - /url: /#faq
- main
```

# Test source

```ts
  1  | import { Page, Locator, expect } from '@playwright/test';
  2  | 
  3  | // TaifaSSO redesigned this step from one combined "Enter Credentials" form
  4  | // (ID + password together) into two separate screens: "Sign In" (National ID
  5  | // or Passport only) followed by its own "Enter Your Password" screen. The
  6  | // public API here (assertPageLoaded + submitCredentials) is kept the same as
  7  | // before so callers don't need to change — submitCredentials now drives both
  8  | // screens internally.
  9  | export class TaifaSsoPasswordPage {
  10 |   readonly page: Page;
  11 |   readonly idNumberInput: Locator;
  12 |   readonly idContinueButton: Locator;
  13 |   readonly passwordInput: Locator;
  14 |   readonly passwordContinueButton: Locator;
  15 | 
  16 |   constructor(page: Page) {
  17 |     this.page = page;
  18 |     this.idNumberInput = page.getByLabel(/national id or passport/i);
  19 |     this.idContinueButton = page.getByRole('button', { name: /^continue$/i });
  20 |     this.passwordInput = page.getByPlaceholder(/enter your password/i);
  21 |     this.passwordContinueButton = page.getByRole('button', { name: /^continue$/i });
  22 |   }
  23 | 
  24 |   async assertPageLoaded() {
  25 |     // 30s rather than 15s — the redirect from RMS login to this page is a
  26 |     // real hop to a separate live SSO service over the tunnel, and has been
  27 |     // observed to intermittently take longer than 15s.
> 28 |     await expect(this.page.getByRole('heading', { name: /^sign in$/i })).toBeVisible({ timeout: 30000 });
     |                                                                          ^ Error: expect(locator).toBeVisible() failed
  29 |     await expect(this.idNumberInput).toBeVisible();
  30 |   }
  31 | 
  32 |   async submitCredentials(idNumber: string, password: string) {
  33 |     await this.idNumberInput.fill(idNumber);
  34 |     await this.idContinueButton.click();
  35 | 
  36 |     await expect(this.page.getByRole('heading', { name: /enter your password/i })).toBeVisible({ timeout: 30000 });
  37 |     await expect(this.passwordInput).toBeVisible();
  38 |     await this.passwordInput.fill(password);
  39 |     await this.passwordContinueButton.click();
  40 |   }
  41 | }
  42 | 
```