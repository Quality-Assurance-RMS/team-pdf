# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UI/Login/login-rmb-tc2-login-with-remember-me.spec.ts >> Login — Remember Me >> TC2 — Login succeeds with Remember Me checked
- Location: tests/UI/Login/login-rmb-tc2-login-with-remember-me.spec.ts:19:7

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/auth\/login/
Received string: "https://api.rms.dev.demo-fsit.com/auth/login"
Timeout: 15000ms

Call log:
  - Expect "not toHaveURL" with timeout 15000ms
    33 × unexpected value "https://api.rms.dev.demo-fsit.com/auth/login"

```

```yaml
- img "Unified Control"
- paragraph: Government services, now at your fingertips
- img
- paragraph: RMS
- paragraph: National Treasury
- heading "Login" [level=1]
- paragraph: Access Revenue Management System (RMS)
- text: Email address
- textbox "Enter Your email": ashil.shaji@fortunesoftit.com
- paragraph
- text: Password
- textbox "Enter Password": RMS@fortune@123
- button
- paragraph
- checkbox "Remember for 30 days" [checked]
- text: Remember for 30 days
- link "Forgot Password?":
  - /url: /auth/forgot-password
- button "Log In →"
- text: Or
- button "Login with SSO →"
- region "Notifications alt+T"
```

# Test source

```ts
  1  | import { test, expect } from '../fixtures';
  2  | import { allure } from 'allure-playwright';
  3  | import { LoginPage } from '../../pages/LoginPage';
  4  | 
  5  | const EMAIL = process.env.LOGIN_EMAIL!;
  6  | const PASSWORD = process.env.LOGIN_PASSWORD!;
  7  | 
  8  | test.describe('Login — Remember Me', () => {
  9  |   test.use({ storageState: { cookies: [], origins: [] } });
  10 | 
  11 |   let loginPage: LoginPage;
  12 | 
  13 |   test.beforeEach(async ({ page }) => {
  14 |     loginPage = new LoginPage(page);
  15 |     await loginPage.goto();
  16 |     await loginPage.assertPageLoaded();
  17 |   });
  18 | 
  19 |   test('TC2 — Login succeeds with Remember Me checked', async ({ page, logger }) => {
  20 |     await allure.description('Verifies that enabling Remember Me before login still allows a successful login.');
  21 |     await allure.label('feature',  'Login');
  22 |     await allure.label('story',    'Remember Me');
  23 |     await allure.label('severity', 'normal');
  24 |     await allure.label('priority', 'P2');
  25 |     await allure.label('testId',   'LOGIN-RMB-TC2');
  26 | 
  27 |     await logger.step('Step 1 — Verifies that enabling Remember Me before login still allows a successful login', async () => {
  28 |       logger.info('Test scope: login success with Remember Me enabled');
  29 |     });
  30 | 
  31 |     await logger.step('Step 2 — Check Remember Me checkbox', async () => {
  32 |       logger.info('Enabling the Remember Me option before login');
  33 |       await loginPage.rememberCheckbox.check();
  34 |       await expect(loginPage.rememberCheckbox).toBeChecked();
  35 |       logger.pass('Remember Me checkbox is checked');
  36 |     });
  37 | 
  38 |     await logger.step('Step 3 — Submit credentials and verify successful login', async () => {
  39 |       logger.info('Submitting valid credentials with Remember Me enabled');
  40 |       await loginPage.login(EMAIL, PASSWORD);
  41 |       logger.info('Verifying redirect away from login page');
> 42 |       await expect(page).not.toHaveURL(/\/auth\/login/, { timeout: 15000 });
     |                              ^ Error: expect(page).not.toHaveURL(expected) failed
  43 |       logger.pass('Login successful with Remember Me enabled — user redirected from login page');
  44 |     });
  45 |   });
  46 | });
  47 | 
```