# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: DB/db-conn-tc1-connectivity.spec.ts >> Database — Connectivity >> TC1 — Can connect to the Konza UAT database and run a query
- Location: tests/DB/db-conn-tc1-connectivity.spec.ts:9:7

# Error details

```
Error: Connection terminated due to connection timeout
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { getKonzaDbPool, closeKonzaDbPool } from '../utils/dbClient';
  3  | 
  4  | test.describe('Database — Connectivity', () => {
  5  |   test.afterAll(async () => {
  6  |     await closeKonzaDbPool();
  7  |   });
  8  | 
  9  |   test('TC1 — Can connect to the Konza UAT database and run a query', async () => {
  10 |     const pool = getKonzaDbPool();
> 11 |     const result = await pool.query('SELECT 1 AS ok');
     |                    ^ Error: Connection terminated due to connection timeout
  12 |     expect(result.rows[0].ok).toBe(1);
  13 |   });
  14 | });
  15 | 
```